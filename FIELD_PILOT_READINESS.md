# 현장 실증 준비도 — 수기·사진·카톡 보고 없애기

> 이 문서는 **계속 갱신하는 작업 상태판**입니다.
> 새 세션은 이 문서를 먼저 읽고 「다음 우선순위」부터 이어서 진행합니다.
>
> 이번 단계는 **새 기능을 늘리는 자리가 아닙니다.** 이미 만들어 둔 것을
> 실제 업무에 맞게 잇고, 현장 기사님이 폰으로 실제로 쓸 수 있게 만듭니다.

---

## 이번 통화(홍현주 이사님)에서 확인된 실제 업무

```
병원 방문 → 수거 → 배출 박스·용기 개수 확인 → 병원별 수기대장 작성
→ 공급 자재 수량 작성 → 대장 사진 촬영 → 하루 끝나고 카카오톡 단체방 전송
→ 이사님이 다시 확인 → Excel 정리
```

없애려는 것은 **뒤쪽 절반**입니다 — 사진 촬영·카톡 전송·이사님 재정리.
병원에 법적·업무상 보관해야 하는 **수기대장 자체는 없앨 수 있다고 임의로
판단하지 않습니다.** 별도 확인 전까지 유지 가능성을 전제로 둡니다.

### 종료 조건

| 대상 | 조건 |
|---|---|
| 현장직원 | 수기대장을 다시 찍어 카톡으로 보고할 필요가 크게 줄었다 |
| 이사님 | 시스템을 열면 오늘 무엇을 수거하고 어떤 자재를 줬는지 바로 안다 |
| 병원 | 용기가 부족하거나 추가수거가 필요할 때 전화 전에 시스템에서 요청한다 |
| 시스템 | 직원 입력이 수거이력·자재·재고·정산자료로 **중복 없이** 이어진다 |

---

## Phase 1 — 이미 있는 것 (감사 결과)

**새로 만들 것이 생각보다 적습니다.** 데이터 모델은 이미 이번 업무를 담고 있습니다.

| 이번 통화의 업무 | 지금 상태 | 근거 |
|---|---|---|
| 수거량 | **있음** | `CollectionCompletionInput.actualAmount` |
| **배출 박스·용기 종류와 개수** | **있음** | `containers` (골판지·합성수지·봉투·기타) |
| **공급한 자재 종류와 개수** | **있음** | `supplied` (재고 차감용) + `suppliedItems` (규격별, 정산용) |
| 특이사항 | **있음** | `memo` + 거래처별 `site_notes` |
| 한 번 입력 → 자동 연결 | **있음** | `complete_collection` 한 트랜잭션에 일정·이력·자재·재고·요청 |
| 이사님 당일 확인 | **있음** | `FieldTodayCard` — 대시보드의 「오늘 현장에서 N건 들어왔습니다」 |
| 병원의 자재·용기 요청 | **있음** | 요청 종류에 `소모품` |
| 고정 수거주기 → 월 일정 자동생성 | **있음** | `schedulePlan.ts` + `create_planned_schedules` (0028) |
| 현장 의견 → 이사님 알림 | **있음 (0062)** | `schedule_feedback` |
| 수량 입력의 큰 [−][+] 버튼 | **있음** | `QtyField` — 이미 44px 하한 |

### 없거나 부족한 것

| # | 무엇 | 상태 |
|---|---|---|
| F1 | **현장 수거 입력이 폰에서 4화면** — 「오늘 일정」에서 병원을 눌러 들어와도 ①일정 선택 ②거래처 선택을 다시 지나감 | **해소 (Phase 2)** |
| ~~F2~~ | ~~「차량·기사」 칸이 폰에서 395px~~ — **코드는 이미 있었습니다**(0056). 계정에 차량을 묶으면 접힙니다 | **해소 (Phase 6) · 남은 것은 설정** |
| ~~F3~~ | ~~이사님 확인화면에 배출 용기·공급 자재가 안 보임~~ | **해소 (Phase 3)** |
| ~~F4~~ | ~~예정인데 아직 입력이 없는 건을 구분해 보여 주는 자리 없음~~ | **해소 (Phase 3)** |
| ~~F5~~ | ~~요청 종류 `소모품`이 **판매 상품처럼 읽힘** — 실제로 급한 것은 「용기 부족」~~ | **해소 (Phase 5)** |
| F6 | 거래처 연락처가 `manager`·`phone` 두 칸뿐 — 보조 연락처·선호 연락방식·담당자 변경 기록 없음 | **남음 (DB 변경 → 승인 필요)** |
| F7 | 거래처별 자재 공급계획(월 사용량·공급주기·보관공간) 구조 없음 | **남음 (DB 변경 → 승인 필요)** |
| F8 | 직원별 기본 담당업무·사용가능 차량·기본 근무시간대 구조 없음 | **남음 (DB 변경 → 승인 필요)** |

---

## Phase 2 — 현장 수거 입력 (진행 중)

### 무엇이 문제였나 (측정값, field 계정 · 폰 390×844)

| | Before | After |
|---|---:|---:|
| 문서 높이 | **3,378px (4.0화면)** | **2,932px (3.5화면)** |
| **수거량 칸 위치** | 두 화면 아래 | **y=506px (첫 화면 안)** |
| 입력칸 수 | 25 | 24 |

기사님이 「오늘 일정」에서 병원을 눌러 들어오면 **어느 병원인지·무슨
폐기물인지는 이미 정해져 있습니다.** 그런데도 ①일정 선택 ②거래처 선택이
그대로 펼쳐져 있어, 수거량 칸까지 두 화면을 밀어야 했습니다.

### 바꾼 것

정해진 것은 **「지금 이 병원」 한 줄**로 접었습니다.

- 병원 이름 · 폐기물 구분 · 방문 시각
- **주소** (그 자리에서 확인)
- **담당자 이름 · 전화번호 — 누르면 바로 걸립니다** (번호를 옮겨 적지 않게)
- 「다른 일정 고르기」로 언제든 펼칩니다

일정 없이 **직접 입력**으로 들어오면 예전 그대로입니다 — 그때는 실제로
고를 것이 있습니다.

### 검증 (`check_fieldflow.mjs`, 11건 전부 통과)

- 「지금 이 병원」으로 접힘 · 이름·주소·전화가 보임 · 다시 고를 길이 남음
- 수거량이 y=506px · 가로 밀림 0 · 직접 입력은 예전 그대로

---

## Phase 3 — 이사님 당일 확인화면 (완료)

### F3 — 카톡 사진을 대신하려면 수거량만으로는 안 됩니다

이사님이 사진방을 다시 여는 이유는 **배출 용기 개수와 공급 자재**입니다.
그 둘이 없으면 이 화면으로 사진을 대신할 수 없습니다. 데이터는 이미
있었고(`containers` · `materials`), 화면에 안 나오고 있었을 뿐입니다.

한 줄에 이렇게 나옵니다.

```
09:40  한마음요양병원  118kg  김준기
       용기 골판지 3 · 합성수지 2 (5개) · 자재 박스 10 · 바늘통 2
       지하 주차 후 화물엘리베이터
```

⚠ **안 적힌 용기를 0 으로 채우지 않습니다.** 「용기 미기재」라고 적습니다 —
「안 적었다」와 「0개였다」는 다른 말이고, 0 으로 채우면 이사님이 그 숫자를
사실로 읽으시게 됩니다.

### F4 — 아직 안 들어온 곳

그날 예정인데 입력이 없는 방문을 **병원·시각·담당자**와 함께 보여 줍니다.
무른 방문(0059)은 세지 않습니다 — 안 가기로 한 것입니다.

⚠ **「누락」이라고 단정하지 않습니다.** 일정이 바뀌었을 수도, 병원이 쉬었을
수도, 내일 처리하기로 했을 수도 있습니다. 시스템은 그 이유를 모릅니다.
제목은 **「아직 입력이 없는 곳 N곳」**이고, 아래에 「'안 했다'가 아니라 아직
입력이 없다는 뜻입니다」라고 적습니다. 검사도 **제목에 「누락·빠뜨림·미이행」이
없는지**를 봅니다.

그리고 예전에는 들어온 것이 하나도 없으면 이 카드를 아예 안 그렸습니다.
이사님이 이 화면을 여는 이유의 절반이 「아직 안 들어온 곳」이라, 둘 다
없을 때만 조용하게 바꿨습니다.

### 검증 (`check_officeday.mjs`, 17건 전부 통과)

용기·합계·자재·특이사항·수거량·기사 표시 · 미입력 목록(병원·시각·담당자) ·
**제목이 「누락」이라 안 함** · 용기 미기재 처리 · **기사님 화면에는 안 뜸**

---

## Phase 5 — 병원 첫 화면 (완료)

### 무엇이 문제였나

병원 첫 화면의 두 번째 큰 버튼이 **「소모품 주문」**이었습니다. 이사님 통화
기준으로 병원이 급한 것은 **용기가 모자란 것**이지 물건을 사겠다는 뜻이
아닙니다. 「소모품 주문」이라고 적어 두면 판매 상품 목록처럼 읽혀서, 정작
용기가 없어 못 버리는 병원이 그 칸을 안 누르고 **전화를 겁니다.**

요청 종류 이름도 같은 문제였습니다 (F5).

### 바꾼 것 — **글자만**

| | Before | After |
|---|---|---|
| 첫 화면 두 번째 버튼 | 소모품 주문 | **자재·용기 요청** |
| 버튼 설명 | 전용 용기 · 봉투 | **전용 용기 · 봉투 · 바늘통이 부족할 때** |
| 요청 종류 이름 | 소모품 | **자재·용기** |
| 병원 투어 안내 | 「추가수거」나 「소모품」 | 「수거 요청」이나 「자재·용기 요청」 |

⚠ **저장값은 그대로 `소모품` 입니다.** DB 의 CHECK 로 굳어 있는 값이라
바꾸려면 migration 이 필요합니다. `REQUEST_KIND_LABEL` 을 한 곳에 두고
**사람에게 보여 줄 때만** 지나갑니다 — 화면·서버가 어긋나지 않게.

버튼이 가는 곳(물품 화면)과 소모품 판매 기능은 **그대로 둡니다.** 없앤 것이
아니라 **파는 것처럼 부르지 않는 것**입니다 (지시 14). 첫 화면에 가격·결제·
구매 같은 말이 없는지도 검사에 넣었습니다.

### 검증 (`check_portalhome.mjs`, 17건 전부 통과)

- 폰 390×844 에서 **두 버튼 다 스크롤 없이** (아래끝 354px · 531px), 둘 다 44px 이상
- 두 번째 버튼이 「자재·용기 요청」이고 「소모품」이라 부르지 않음
- 누르면 실제로 물품 화면으로 감
- 요청 창 종류가 `긴급수거 · 추가수거 · 자재·용기 · 교육·자료 · 기타`
- **서버로 가는 값은 그대로 `소모품`** ← 이번 변경의 핵심
- 저장값이 `소모품` 인 지난 요청도 화면에서는 「자재·용기」
- 첫 화면에 가격·결제·구매 같은 말 없음

---

## Phase 6 — 현장 권한 재검증 · F2 (완료)

### 왜 화면을 다시 쟀나 — RLS 만으로 안 막히는 것이 있습니다

서버 정책을 그대로 읽어 보면 **현장 토큰으로도 실제로 내려오는 돈**이 있습니다.

| 표 | 정책 | 현장에 오는가 | 들어 있는 돈 |
|---|---|---|---|
| `clients` | `clients_read = is_active_user()` | **예** | `pricing` · `monthly_flat_fee` |
| `client_monthly_actuals` | `cma_read = is_active_user()` | **예** | `revenue` · `cost` · `profit` |
| `payments` | `is_staff()` | 아니오 (0건) | — |
| `sales_leads` | `is_staff()` | 아니오 (0건) | — |

그래서 감사는 **서버가 실제로 주는 것만** 흘려 넣습니다. 안 주는 것을 넣어
두고 「안 보인다」고 하면 아무것도 증명하지 못합니다.

### 잡은 것 — 「연결되는 매출」 (PRODUCT DEFECT)

`/requests` 는 현장도 여는 화면입니다(병원이 올린 요청을 보고 가야 하니까).
그런데 요청마다 **「연결되는 매출: 추가 수거 매출」**이 함께 붙어 있었습니다.
금액은 아니지만 **영업 정보**입니다.

화면을 막지 않고 **그 한 줄만** 뺐습니다 — 요청 내용·상태·회신은 그대로입니다.
시연(로그인 없음)에서는 그대로 보입니다.

### F2 — 새로 만들 것이 없었습니다

「차량·기사」 395px 는 **기능이 없어서**가 아니라 그때 잰 계정에 **차량이 안
묶여 있어서**였습니다. 0056 의 `vehicleHidden` 이 이미 한 줄로 접습니다.
그래서 만들지 않고 **묶었을 때 실제로 줄어드는지** 두 상태를 나란히 쟀습니다.

| | 안 묶인 계정 | 묶인 계정 |
|---:|---:|---:|
| 차량 칸 | **247px** | **93px** |
| 문서 전체 | 2,785px | **2,631px (3.1화면)** |

⚠ 제가 잡았던 2,600px 에 **31px 못 미칩니다.** 통과시키려고 기준을 2,631 로
내리지 않았습니다 — 그건 자를 고치는 것입니다. 검사는 **다시 늘어나는 것**을
막는 자리(2,700px)로 두고, 남은 31px 은 사실대로 적습니다.

⚠ 앞서 적힌 2,932px 이 2,785px 로 줄어 보이는 것은 화면이 바뀌어서가 아니라,
이번 fixture 에는 차량이 등록돼 있어 「등록된 차량이 없어 저장할 수 없습니다」
안내가 안 뜨기 때문입니다.

### 직원별 당일 점검 (통화 지시 항목)

이사님 화면에 사람별 한 줄을 붙였습니다.

```
김준기  3곳 들어옴        오대성  0곳 들어옴 · 1곳 아직
```

⚠ **성적표가 아닙니다.** 「N곳 아직」은 「안 했다」가 아니라 「아직 입력이
없다」입니다. 숫자에 빨간색도 안 씁니다 — 이름 옆에 붙는 색은 곧 판정이
됩니다. 두 이름의 출처가 다르다는 것도 코드 주석에 박아 뒀습니다:
`들어옴` 은 수거 입력에 **사람이 적은** 기사 이름, `아직` 은 일정에 **미리
적어 둔** 담당 이름입니다. 대체로 갔으면 갈리는데, 한쪽으로 맞추면 **실제로
간 사람이 지워집니다.** 기사가 한 명뿐이면 안 그립니다.

### 검증

| 스위트 | 결과 |
|---|---|
| `check_fieldperm.mjs` (신규) | **71건 전부 통과** — 현장 화면 7곳 × 폰/PC 전수 · 막힌 화면 13곳 · 대체기사 · 사무실 대조 |
| `check_fieldflow.mjs` | **18건 전부 통과** (F2 측정 6건 추가) |
| `check_officeday.mjs` | **23건 전부 통과** (직원별 줄 6건 추가) |
| `check_flow390.mjs` | **53건 전부 통과** (폰 하루 흐름 · 실서버 규칙 그대로) |
| `db_perm.mjs` | **54건 전부 통과** (서버가 실제로 막는지) |
| `db_roles_e2e.mjs` | **45건 전부 통과** (진짜 RLS 위에서 현장·병원·사무실의 하루) |
| 전체 브라우저 회귀 | **80 스위트 · 2,286 검사 · 실패 0 · 깨진 스위트 0** |
| 전체 DB 회귀 | **38 스위트 · 1,321 검사 · 실패 0 · 깨진 스위트 0** |



### 이번에 제가 낸 검사 결함 두 개 (기록)

1. **`vehicles` fixture 에 `active` 를 빠뜨림** — `repo.ts:507` 이
   `filter(v => v.active)` 로 갈라 담아서 **차량이 0대인 상태**가 됐고,
   「묶어도 안 접힌다」는 실패가 났습니다. 원인은 제품이 아니라 fixture 였습니다.
   이제 「고를 차가 실제로 있는가」를 먼저 잽니다.
2. **`site_notes` 칸 이름을 기억으로 씀** (`body`/`author_name`/`pinned`) —
   실제로는 `content`/`kind`/`done`/`archived` 입니다. 그 병원 화면이 통째로
   ErrorBoundary 에 앉았는데, **그 상태로도 「금액 없음」은 통과**했습니다.
   터진 화면은 당연히 금액이 없습니다. 그래서 「열렸는가」를 먼저 잽니다.

---

## Phase 6-B — 현장 계정에서 돈을 **서버가** 막습니다 (0063 · 대표님 지시 HIGH)

### 무엇이 문제였나 — 화면만 가리고 있었습니다

Phase 6 에서 화면을 전수로 훑어 「현장에 돈이 안 보인다」를 확인했습니다.
그런데 그건 **화면을 쓸 때만** 맞는 말이었습니다. 서버 정책을 그대로 읽어
보면 기사 계정 토큰만 있으면 표를 직접 불러 받아 갈 수 있었습니다.

| 무엇 | 어디에 | 왜 새고 있었나 |
|---|---|---|
| **품목별 단가 · 월정액** | `clients.pricing` · `monthly_flat_fee` | `clients_read` 가 `is_active_user()` — 행이 오면 **그 행의 모든 칸**이 옵니다 |
| **결제조건** | `clients.payment_terms` · `payment_due_day` | 〃 |
| **세금계산서 정보** | `clients.biz_*` · `tax_email` · `vat_mode` | 〃 |
| **거래처별 월 매출·원가·이익** | `client_monthly_actuals` | `cma_read` 가 `is_active_user()` |
| **판매가 · 매입원가** | `products` | `products_read` 가 `auth_role() is not null` — 병원 보라고 연 문이 기사에게도 열림 |
| **단가표 문서** | `client_documents` (kind='단가표') | `client_documents_read` 가 `is_active_user()` |

### 왜 RLS 로 못 막았나

**RLS 는 행만 막습니다.** 「이 행은 보되 이 칸은 가려라」를 쓸 수 없습니다.
그래서 표 단위로 막을 수 있는 것은 정책으로, 거래처처럼 **한 표 안에 업무
정보와 돈이 섞여 있는 것은 열 권한**으로 잠갔습니다.

⚠ PostgreSQL 은 **표 권한이 있으면 열 권한 회수가 아무 일도 안 합니다.**
그래서 표 권한을 먼저 회수하고 안전한 칸만 다시 내줍니다. 순서가 바뀌면
막은 줄 알았는데 안 막힙니다 — 격리 DB 에서 실제로 확인했습니다.

### 무엇을 했나 (0063 = RUN_49)

| | |
|---|---|
| `client_monthly_actuals` · `client_documents` 읽기 | → `is_staff()` |
| `products` 읽기 | → `is_staff()` 또는 **병원 본인** (기사만 빠짐) |
| `clients` | 표 읽기 권한 회수 → **업무 칸만** 다시 내줌 |
| 사무실·대표의 단가 통로 | `client_billing_terms()` (SECURITY DEFINER · `is_staff()`) |
| `complete_collection` | 거래처를 통째로 읽던 자리를 `id`·`name` 만 읽게 |

**주소·담당자·전화·주의사항·수거주기·계약기간은 그대로 열려 있습니다.**
대체 기사가 대신 가도 그것으로 일합니다.

### ⚠ 하마터면 현장 업무를 통째로 막을 뻔했습니다

`complete_collection` 은 **security definer 가 아닙니다** — 부르는 사람
권한으로 돕니다. 그런데 그 안에서 `select * from clients` 를 하고 있었습니다.
돈 칸을 닫는 순간 **기사님이 수거를 저장하지 못하게 됩니다.** 실제로 쓰는
것은 `id` 와 `name` 둘뿐이라 그 둘만 읽게 고쳤고, 검사에 **「기사가 수거를
그대로 저장하는가」**를 넣었습니다 — 막는 것만 확인하면 「다 막아도 통과」
하는 검사가 됩니다.

### 앱도 함께 바뀌었습니다

- 거래처를 `select('*')` 로 읽던 것을 **안전한 칸 목록**으로 (`CLIENT_COLS`)
- 사무실·대표는 돈 칸을 `client_billing_terms()` 로 받아 붙임 (`applyTerms`)
- 거래처 상세의 **「월간 리포트」 탭을 현장에서 내림** — 서버가 월 실적을 안
  주므로 탭만 남기면 빈 표가 뜨고, 그건 「기록이 없다」로 읽혀 더 나쁩니다
- `EXPECTED_SCHEMA_VERSION` 62 → **63**

⚠ **앱과 SQL 은 같은 배포여야 합니다.** 예전 앱 + 새 SQL 이면 별표가 막혀
모든 역할에서 거래처 화면이 안 뜹니다.

---

## Phase 7 — 역할 토큰 E2E (`db_roles_e2e.mjs`)

**진짜 Postgres 에 진짜 RLS 를 켠 채 역할 토큰으로 하루를 밟습니다.**
화면 검사는 서버를 흉내 낸 가짜 응답을 쓰기 때문에 「화면이 안 보여 준다」
까지만 증명합니다.

```
현장   오늘 갈 곳 → 병원 정보(돈 빼고) → 수거 저장 → 자재 차감 → 의견 내기
병원   우리 병원만 → 수거 요청 → 자재·용기 주문 → 남의 것은 못 봄
사무실 같은 데이터가 그대로 다 보임
```

⚠ **이것은 비밀번호로 로그인하는 E2E 가 아닙니다.** 계정과 비밀번호는
대표님이 정하실 일이고, 비밀번호는 코드·픽스처·문서에 적지 않습니다.
서버가 역할별로 무엇을 주고 막는지는 **토큰 수준에서 같습니다** — Supabase 도
로그인 뒤에는 같은 claims 로 판단합니다. 실계정 로그인 E2E 는 계정을 주시면
그때 돌립니다.

---

## Phase 8 — 폰 390px 하루 흐름 (실서버 규칙 그대로)

### 이 검사가 앞의 것들과 다른 점

지금까지의 화면 검사는 **가짜 서버가 현장에게도 돈을 그대로 줬습니다.**
그건 「화면이 가리는가」만 잰 것이고, **서버가 안 줄 때 화면이 멀쩡한가**는
한 번도 안 재 본 셈이었습니다. 0063 뒤에는 그게 실제 상황입니다.

`check_flow390.mjs` 의 가짜 서버는 **0063 적용 뒤의 진짜 서버처럼** 굽니다.

- 거래처를 `select=*` 로 물으면 **실제로 `permission denied` 를 돌려줍니다**
  → 앱이 무심코 별표를 쓰면 **실서버에서 터지기 전에 여기서 터집니다**
- 현장에게 `client_billing_terms()` 는 `[]`, 월실적·상품·문서함도 `[]`
- `clients_full()` 은 관리자만

그리고 매 화면마다 **「앱이 못 읽는 것을 묻지 않았는가」**를 함께 셉니다.

### 밟은 흐름 (46건 전부 통과)

```
현장 390×844
  오늘 일정        갈 곳·시각이 보임 · 가로 밀림 0px
  → 병원 누름      「지금 이 병원」 한 줄 · 주소·전화가 그 자리에
  → 수거량 118kg
  → 용기           골판지 3 · 합성수지 2   (＋ 49×49px)
  → 자재           「다른 규격 13개 보기」로 펴서 63L 박스 10  (＋ 49×49px)
  → 저장           75px 버튼 · 서버로 그대로 감

병원 390×844
  첫 화면          ① 수거 요청 354px · ② 자재·용기 요청 531px (둘 다 첫 화면 안)
  → 수거 요청      우리 병원 것으로 · 종류 추가수거 · 적은 내용 그대로
  → 자재·용기      물품 화면 → 10개 → 주문 감 (＋ 45×59px)
```

⚠ **보낸 것에 금액이 한 칸도 없습니다.** 현장도 병원도 금액을 만들지
않습니다 — 서버가 그 시점 단가로 굳힙니다. 그것도 검사에 넣었습니다.

### TEST HARNESS DEFECT (제 잘못)

자재 칸을 펴는 버튼을 `/더 보기|모두|펼치/` 로 찾다가 못 찾고 **「자재 칸이
폰에서 안 닿는다」고 잘못 보고할 뻔했습니다.** 실제 문구는 「다른 규격 N개
보기」입니다. 이름표(`data-supply-more`)로 집도록 고쳤고, **접혀 있는 것이
설계대로인지**(공급 이력이 없는 병원은 열 줄이 다 접힙니다)까지 함께
검사하게 했습니다.

---

## 실서버 점검 — `supabase/checks/CHECK_49_field_money.sql`

0063 을 올린 **운영 DB 에서 직접** 확인하는 읽기 전용 스크립트입니다.
SQL Editor 에 붙여 넣고 실행하면 `결과` 칸이 전부 `통과` 여야 합니다.

- 운영 DB 에 **실제로 등록된 계정**을 역할별로 하나씩 골라, 그 계정의
  토큰으로 로그인한 것과 **같은 조건**에서 표를 직접 불러 봅니다
- **비밀번호를 쓰지 않습니다**
- insert/update/delete 가 한 줄도 없고, 전체가 한 트랜잭션이며 **rollback**

⚠ 막혔는지만 보면 「다 막아도 통과」하는 점검이 됩니다. 그래서 넷은
**안 막혔는지**를 봅니다 — 현장의 주소·연락처, 병원의 상품 목록,
사무실의 단가, 관리자의 백업.

---

## 실서버 확인 완료 — CHECK_49 (2026-08-18)

대표님이 운영 DB 에서 `CHECK_49_field_money.sql` 을 실행해 **전부 통과**했습니다.
「현장에는 돈이 안 보인다」가 이제 **화면이 아니라 서버에서** 지켜집니다.

실제 등록된 계정의 토큰 조건에서 확인한 것 — 현장은 단가·`select *`·월실적·
상품·문서함·청구가 막히고 주소·연락처·주의사항은 읽히며, 병원은 자기 병원과
상품 목록만, 사무실은 단가를 그대로 받고, 관리자 백업은 거래처를 통째로 받습니다.

---

## Phase 9 — 거래처 상세를 전화 받으면서 쓸 수 있게

이사님이 병원 전화를 받으면 이 화면을 엽니다. 폰에서 재 보니:

| | Before | After |
|---|---:|---:|
| 문서 길이 | 3,284px (3.9화면) | **2,877px (3.4화면)** |
| **탭 줄** (수거이력·자재·요청) | **2,103px (2.5화면)** | **1,695px (2.0화면)** |
| 「다음 행동 AI 추천」 덩어리 | 800px | **293px** |
| 전화번호 | 389px | 389px (그대로) |
| 주의사항(현장 메모) | 626px | 626px (그대로) |

폰에서 추천 카드를 **접었습니다**(PC 는 지금까지처럼 전부 보입니다).
근거 안내문(96px)도 펼쳤을 때만 나옵니다 — 접힌 채로도 늘 자리를 먹고
있었습니다.

⚠ **추천을 지운 것이 아닙니다.** 「추천 N건 보기」 한 번이면 그대로 나오고,
펼치면 근거 안내문도 함께 나옵니다. 그것도 검사에 넣었습니다.

### 그 과정에서 나온 것 — 화면 하나가 통째로 죽던 자리

거래처 상세를 재려다 **ErrorBoundary 로 떨어졌습니다.**
`noteMeta[note.kind]` 가 undefined 면 `.icon` 에서 터져 **화면 전체**가 안 뜹니다.

원인은 제 검사 픽스처였습니다(`site_notes` 의 칸은 `kind`·`content` 인데
다른 이름을 넣었습니다). **운영 데이터로는 날 수 없습니다** —
`site_notes_kind_check` 가 다섯 값만 허용합니다. 그게 진짜 방패입니다.

다만 나중에 종류를 하나 늘리면서 이 표를 같이 안 고치면 **메모 한 줄 때문에
화면 전체가** 안 뜹니다. 대가가 너무 커서 모르는 종류는 「기타」로 그리되
**적힌 글자는 그대로 보여 주도록** 했습니다. 감추지 않습니다.

---

## Phase 10 — 월말 청구 폰 · 빈 상태 문구

### 월말 청구 (M2)

**돈이 굳는 화면입니다.** 그래서 경고는 하나도 접지 않았습니다 —
「되돌릴 수 없다」와 「기본 단가로 계산됨」은 그대로 두고, **지금 눌러야 할
것이 아닌 것만** 접었습니다.

| | Before | After |
|---|---:|---:|
| 문서 길이 | 5,526px (6.5화면) | **4,925px (5.8화면)** |
| **「확정하기」 버튼** | **3,004px (3.6화면)** | **2,403px (2.8화면)** |
| 「사람만 아는 두 가지」 | 530px | **249px** (폰에서 접힘) |
| 달 고르기 | 382px | 288px (두 칸 격자) |

「사람만 아는 두 가지」(명세서 보냄·세금계산서 발행)는 **확정 뒤에** 하는
일입니다. 폰에서만 접되 **「0 / 2 표시함」은 접혀도 보입니다** — 안 보이면
「나중에 하지」가 아니라 「한 줄이 사라졌다」가 됩니다.

⚠ 이 과정에서 **검사가 헛돌 뻔했습니다.** 접는 칸을 두 벌로 그렸더니
`data-mark-row` 가 DOM 에 두 번 생겨, 검사가 늘 숨은 쪽을 집어 「접혀 있다」가
**항상 통과**했습니다. 한 벌만 그리도록 고치고, **DOM 에 하나만 있는지**를
검사 항목으로 넣었습니다.

### 빈 상태 문구 (M7)

문구 자체는 이 저장소가 스스로 정한 규칙을 지키고 있었습니다 — 「무엇이
비었다」가 아니라 **「그래서 무슨 일이 벌어진다」**를 적습니다.

```
청구가 있는 1곳 중 1곳에 사업자등록번호가 없습니다.
그 병원은 세금계산서 발행 자료에서 「확인 필요」로 빠집니다
— 결국 홈택스에 손으로 넣게 됩니다.
```

진짜 결함은 **같은 말을 두 번** 하던 것이었습니다. 차량 경고가 설정 카드
설명과 그 안 `VehicleManager` 에서 연달아 두 번 떴습니다. 뒤엣것에서
중복 문장을 빼고 **채울 때 필요한 안내**(의료·기저귀 차량 구분)만 남겼습니다.

⚠ 나머지 빈 상태는 **사실이라 지우지 않습니다.** 실증 초기에 비어 있는 것을
「채워진 것처럼」 보이게 만들 이유가 없습니다.

---

## Phase 11 — 병원 수거 이력 (표 → 카드)

인증·실사 때 병원이 **직접 쓰는 자료**입니다. 폰에서 재 보니 표가 640px 인데
칸은 354px — **가로로 286px 밀렸습니다.** 「용기」와 「처리장 인계」를 보려면
옆으로 밀어야 했고, 밀 수 있다는 표시도 없었습니다.

폰에서는 카드 한 장에 날짜·구분·인계 상태 / 배출량 / 용기 를 담습니다.
넓은 화면은 표 그대로입니다 — 여러 줄을 견주는 데는 표가 낫습니다.

### 그 과정에서 나온 실제 결함

용기 칸을 `{r.containerType} {r.containerCount}개` 로 그리고 있었습니다.
`containerType` 은 **null 이 될 수 있습니다**(현장에서 안 적었을 때).
그러면 병원 화면에 **「개」 한 글자만 덩그러니** 떴습니다.

「용기 미기재」로 고쳤습니다. **0 으로 채우지 않습니다** — 「안 적었다」와
「0개였다」는 다른 말이고, 병원은 이 숫자를 사실로 읽습니다.

### 안 바꾼 것과 그 이유

`/history`(수거이력)는 1,513px · 11칸으로 가장 심하지만 **표로 남깁니다.**
카드로 바꾸면 `data-history-revert` 가 DOM 에 두 벌 생깁니다 — 바로 앞
월말 청구에서 정확히 그 함정에 빠져 「접혀 있다」가 **항상 통과**하던 검사를
만들었습니다. 게다가 사무실·관리자 전용이고, 전화 중 확인은 이미 고쳐 둔
거래처 상세의 수거이력 탭이 맡습니다.

---

## Phase 12 — 0063 뒤 **청구 금액이 1원도 안 달라졌는가**

### 왜 이 검사가 따로 필요했나

0063 은 거래처의 단가·월정액을 `clients` 행에서 **떼어 냈습니다.**
앱은 이제 `client_billing_terms()` 로 따로 받아 붙입니다(`applyTerms`).

그런데 지금까지의 돈 검사 60여 개는 전부 **0063 이전 모양**을 씁니다 —
거래처 행에 `pricing` 을 얹어 놓습니다. 실서버는 더 이상 그 칸을 주지
않습니다. **즉 붙이는 과정이 깨져도 검사가 못 잡습니다.** 단가를 못 받으면
그 거래처 청구는 조용히 기본 단가로 계산되고, 그 금액이 그대로 병원에
나갑니다.

### 어떻게 했나 — 픽스처 60개를 고치는 대신 대조

같은 자료를 **두 모양**으로 돌려 화면에 뜬 금액이 **글자까지 같은지** 봅니다.

| 모양 | 거래처 행 | RPC |
|---|---|---|
| 옛 (0063 이전) | `pricing` 있음 | 빈 배열 |
| **새 (지금 서버)** | **없음** | **단가를 줌** |
| 못 받음 (일부러) | 없음 | 빈 배열 |

⚠ 금액을 **글자 그대로** 견줍니다. 숫자로 바꿔서 비교하면 `1,000,000` 과
`1000000` 이 같아 보여 표기가 깨진 것을 못 잡습니다.

### 결과 (`check_terms.mjs`, 15건 전부 통과)

```
옛 A병원 1,000,000원 · 합계 10,000,000원   (100만 + 월정액 900만)
새 A병원 1,000,000원 · 합계 10,000,000원   ← 글자까지 같음
못 받음   950,000원                        ← 기본 단가 950원으로 떨어짐
```

⚠ **이 검사가 실제로 무는지**도 함께 봅니다. 일부러 단가를 못 받게 했을 때
금액이 안 달라지면 위 두 줄은 아무것도 증명하지 못한 것입니다.

### 마지막 방패 — 단가를 못 받아도 조용히 청구되지 않는다

| | 정상 | 단가를 못 받음 |
|---|---|---|
| 기본 단가 경고 | 안 뜸 | **뜸** |
| 그 거래처 체크 | 켜짐 | **꺼진 채로 시작** |
| 거래처 줄 | — | 「기본 단가 · 의료폐기물」이라고 적힘 |

이사님이 「전체 선택」을 누르지 않는 한 **추정 금액이 병원에 나가지 않습니다.**

---

## Phase 13 — 100% 까지 남은 것 (딱 두 가지)

readiness 를 100% 로 올리려면 제 손이 아니라 **대표님 손이 필요한 것**이
둘 남았습니다. 나머지는 이번 회차로 닫았습니다.

### ① 실계정 비밀번호 로그인 — **준비 끝, 계정만 주시면 즉시**

`check_live_login.mjs` 를 만들어 두었습니다. 계정을 넣어 한 줄 돌리면 됩니다.

```
E2E_FIELD_EMAIL=... E2E_FIELD_PW=... \
E2E_CLIENT_EMAIL=... E2E_CLIENT_PW=... \
node check_live_login.mjs
```

- **비밀번호는 환경변수로만 받습니다.** 코드·픽스처·문서·git 어디에도
  적지 않습니다. 화면 출력에서도 이메일을 가립니다(`so***@…`).
- ⚠ **운영 DB 에 한 줄도 쓰지 않습니다.** GET 이 아닌 요청은 브라우저 단에서
  **전부 막습니다.** 「아마 읽기일 것」이라고 봐주지 않습니다 — 봐준 것이
  운영 자료를 건드립니다. 막힌 것이 있으면 그것도 결과에 적습니다.
- 확인하는 것: 로그인 성공 · 승인/활성 상태 · **역할에 맞는 첫 화면** ·
  현장은 금액이 한 글자도 없고 미수금 화면이 실제로 막힘 · 병원은 수거 요청과
  자재·용기 요청이 있고 포털 밖은 안 열림

계정이 없으면 **「건너뜀」 한 줄을 남기고 통과**시킵니다. 「검사 0」으로
조용히 사라지면 안 돌았다는 사실이 회귀 집계에서 안 보이기 때문입니다.

### ② 직원·차량 데이터 (F8) — DB 변경 승인

직원별 기본 담당업무·사용가능 차량·기본 근무시간대를 담을 자리가 없습니다.
schema 변경이라 제가 임의로 못 합니다. 승인해 주시면 migration 파일까지
만들어 드리고, 실행은 지금까지처럼 대표님이 하십니다.

### 이번 회차로 닫은 것 — 청구 금액 안전 100%

월말 청구뿐 아니라 **거래처 상세의 정산·명세서**까지 0063 전후 금액이
글자까지 같은 것을 확인했습니다. 실제로 병원에 나가는 금액이 그것입니다.

⚠ 정산 탭은 **이번 달**부터 엽니다. 달을 안 옮기면 「집계할 것이 없다」만
나오고 **빈 금액끼리 「같다」**가 됩니다 — 실제로 그렇게 공허하게 통과할
뻔했습니다. 달을 옮겨 실제 금액으로 견줍니다.

---

## 남은 우선순위

### 다음 회차 (효과 큰 순서)

1. **수거 입력 남은 31px** — 묶인 계정 2,631px → 2,600px
2. **고정 수거주기 → 월 일정 → 월 자재 공급계획** 잇기 (F7 승인 후)
3. **실계정 E2E** (계정 대기)

### 대표님이 지금 하실 수 있는 것 (개발 아님)

- **사용자 관리에서 기사님 계정에 차량을 묶어 주세요.** 그러면 수거 입력의
  「차량·기사」 칸이 247px → 93px 로 접히고, 매번 고르지 않아도 됩니다.
  틀린 차로 나간 날은 「오늘은 다른 차로 갔어요」로 그 자리에서 바꿉니다.

---

## 대표님 확인이 필요한 것

| # | 무엇 | 왜 |
|---|---|---|
| 1 | **F6 거래처 연락처 확장** (보조 연락처·선호 연락방식·담당자 변경일) | DB schema 변경 |
| 2 | **F7 거래처별 자재 공급계획** (월 사용량·공급주기·보관공간 메모) | DB schema 변경 · 근거 없는 자동수량은 만들지 않고 참고값까지만 |
| 3 | **F8 직원 기본 담당·사용가능 차량·근무시간대** | DB schema 변경 |
| 4 | 요청 종류에 「인증/점검 대응」 추가 | 업무 의미 변경 |
| 5 | 수기대장을 실제로 줄여도 되는지 (법적·업무상 보관 의무) | 제가 판단하지 않습니다 |

---

## AX 측정 — 지금 잴 수 있는 것 / 기록만 하면 되는 것

| 지표 | 상태 |
|---|---|
| 수거 1건 입력시간 | **자동 측정 중** (`inputDurationMs`) |
| 시스템으로 자동 연결된 건수 | **자동** (`autoPerInput`) |
| 당일 입력 완료율 | F4 를 만들면 자동 |
| 자재 추가배송 횟수 · 긴급 자재 요청 | 요청 종류로 자동 (F5 정리 후) |
| 긴급수거 건수 · 예정 외 방문 | **자동** |
| 직원 수기/사진 보고 건수 · 카톡 보고 횟수 | **사람이 세어야 합니다** (Before 값) |
| 이사님 재정리 시간 | **사람이 재야 합니다** (Before 값) |

추정값을 실제 성과처럼 표시하지 않습니다.

---

## Field Pilot Readiness — **91%**

| 항목 | 현재 | 근거 |
|---|---|---|
| 현장 수거 입력 | **85%** | 폰 하루 흐름을 **실서버 규칙 그대로** 끝까지 통과 · 비밀번호 로그인만 남음 |
| 이사님 당일 확인 | **85%** | 용기·자재·특이사항·미입력·직원별까지 한 화면 · 거래처 상세 탭 2.0화면 · 비밀번호 로그인만 남음 |
| 병원 요청 | **90%** | 수거 요청·자재 주문을 폰에서 끝까지 보냄 · 수거 이력이 폰에서 카드로 · 비밀번호 로그인만 남음 |
| 직원·차량 데이터 | **40%** | 차량 묶기 검증 완료 · 담당·근무시간대 없음(F8) |
| 서버 권한 | **100%** | **운영 DB 에서 CHECK_49 전부 통과** — 화면이 아니라 서버가 막습니다 |
| 청구 금액 안전 | **100%** | 월말 청구 + **정산·명세서**까지 0063 전후 금액이 **글자까지 같음** · 단가를 못 받으면 경고 뜨고 자동 확정 안 됨 |
| 역할 토큰 E2E | **95%** | 서버·화면 양쪽 + **실계정 검사 준비 완료** (`check_live_login.mjs`) · 계정만 주시면 즉시 |

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-18 | 정산·명세서까지 금액 대조 확장(청구 금액 안전 100%) · **실계정 로그인 검사 준비 완료**(쓰기 차단·비밀번호 환경변수) |
| 2026-08-18 | **0063 전후 청구 금액 대조** — 글자까지 같음 확인 · 단가를 못 받을 때의 방패까지 (check_terms 15건) |
| 2026-08-18 | 월말 청구 폰 확정 버튼 3,004 → 2,403px · 빈 상태 중복 문구 · 병원 수거이력 표 → 카드(「개」만 남던 자리 수정) |
| 2026-08-18 | **CHECK_49 실서버 전부 통과** · 거래처 상세 폰 탭 2,103 → 1,695px · 메모 종류 미상일 때 화면이 죽지 않게 |
| 2026-08-18 | **폰 390px 하루 흐름** — 실서버 규칙 그대로인 가짜 서버로 현장·병원 끝까지 (46건). 운영 DB 점검 스크립트 CHECK_49 |
| 2026-08-18 | **0063 — 현장 계정에서 돈을 서버가 차단** (단가·월정액·결제조건·세금정보·월 실적·상품가·단가표). 역할 토큰 E2E 추가 |
| 2026-08-18 | Phase 6 완료 — 현장 화면 전수 감사(71건). **「연결되는 매출」 누출 1건 잡아 제거**. F2 는 코드가 아니라 설정이었음. 직원별 당일 한 줄 추가 |
| 2026-08-18 | Phase 5 완료 — 병원 첫 화면 「자재·용기 요청」. **저장값 `소모품` 은 그대로** (글자만) |
| 2026-08-18 | Phase 3 완료 — 이사님 화면에 용기·자재·특이사항·미입력. 「누락」이라 단정하지 않음 |
| 2026-08-18 | Phase 1 감사 + Phase 2 착수 — 수거 입력에서 「지금 이 병원」으로 ①②를 접음. 수거량 y=1,300대 → **506px**, 문서 3,378 → 2,932px |
