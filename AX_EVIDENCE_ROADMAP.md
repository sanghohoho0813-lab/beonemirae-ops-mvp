# AX 증거 지도 — 네 문장을 숫자로 증명하기까지

> **이 문서는 「무엇을 더 만들까」가 아니라 「지금 무엇을 숫자로 말할 수 있나」를 관리합니다.**
>
> 최종 목적은 기능이 많은 시스템이 아니라 아래 네 문장을 **실제 숫자**로 증명하는 것입니다.
>
> ① 같은 업무를 더 적은 시간으로 처리했다
> ② 기존 병원에서 새로운 상품매출이 실제 발생했다
> ③ 같은 기사·차량으로 더 많은 거래처를 처리할 수 있게 됐다
> ④ 병원이 전화·카톡 대신 직접 BUSINESS AX를 사용하기 시작했다

**「개발 완료」와 「실제 성과 검증 완료」는 다른 말입니다.** 이 문서는 그 둘을 절대 섞지 않습니다.

---

## 0. 지금 상태 한 줄

| 갈래 | 계산할 수 있는가 (개발) | 실제 데이터가 있는가 (실증) |
|---|---|---|
| ① 업무 AX | **가능** — `lib/performance.ts` | **부족** — 실제 현장 표본 30건 필요, 파일럿 대기 |
| ② 매출 AX | **가능** — `lib/axEvidence.ts` `salesAx` | **없음** — 판매가·매입가 미결정, 주문 0건 |
| ③ 확장 AX | **가능** — `lib/axEvidence.ts` `capacityAx` | **없음** — 기사 1명·병원 1곳으로는 처리능력을 못 잽니다 |
| ④ 고객 AX | **가능** — `lib/axEvidence.ts` `customerAx` | **없음** — 병원 포털 실사용 0건 |

**지금 가장 약한 증거: ② 매출 AX와 ④ 고객 AX** — 계산은 되는데 **넣을 실제 기록이 없습니다.**
이것은 개발로 해결되지 않습니다. **파일럿을 돌려야 채워집니다.**

---

## 1. 이 시스템이 지키는 규칙 (숫자를 만들지 않기)

1. **없으면 `null`, 0 이 아닙니다.** `0` 은 「해 봤는데 없었다」이고 `null` 은 「아직 못 셌다」입니다.
   심사 자리에서 이 둘을 섞으면 그때부터 모든 숫자를 의심받습니다.
2. **표본 수를 값과 함께 냅니다.** 5건 미만은 「측정 중(참고값)」, 현장 30건 이상이라야 대표 성과값.
3. **네 단계를 합치지 않습니다** — 주문 접수 → 전달 완료 → 청구 확정 → 입금 완료.
   *주문요청 ≠ 매출, 전달 ≠ 입금.*
4. **거리(km)·소요시간은 계산하지 않습니다.** 거래처 좌표가 없습니다. 지어낸 km 는 판단을 망칩니다.
5. **새 표를 만들지 않습니다.** 같은 뜻의 값을 「보여 주려고」 다시 저장하면 두 숫자가 언젠가 갈라집니다.
6. **Before 를 지어내지 않습니다.** 「도입 전 포털 비율은 당연히 0%」는 추정이지 측정이 아닙니다.
   같은 계산을 도입일 **앞 기간(같은 길이)** 에 그대로 돌립니다.
   그 기간에 시스템 기록이 **통째로 없으면** 그 칸은 `null`(「기록 없음」)입니다 — 0 이 아닙니다.
   반대로 그 기간에 시스템을 쓴 흔적이 있으면 그때의 0 은 **재 봤더니 0** 이므로 그대로 둡니다.
7. **변화를 백분율로 부풀리지 않습니다.** 「0 → 3」을 「＋300%」나 「＋∞」로 적지 않습니다.
   한쪽이 없으면 「견줄 수 없음」입니다.

---

## 1-2. 도입 전 → 현재 (`axCompare`)

`/performance` 맨 위 「도입 전 → 현재」 표. 견주는 항목은 네 갈래에서 대표 하나씩:

당일 입력 완료율 · 소모품 판매매출 · 전달 완료 주문 · 포털 비율 · 포털을 쓴 병원 수 · 하루 방문 병원 수

| 상태 | 뜻 |
|---|---|
| `no-start` | 도입일(실증 시작일)이 설정되지 않음 → **견주지 않습니다** |
| `no-before` | 도입 전 기간에 시스템 기록이 없음 → 그 칸은 「기록 없음」 |
| `no-after` | 도입 후에 아직 기록이 없음 → 파일럿이 시작되면 채워집니다 |
| `ok` | 앞뒤 다 잼 |

⚠ 비교 기간은 **길이를 맞춥니다.** 도입 후 30일과 도입 전 90일을 견주면 건수는 당연히 도입 전이 많습니다.

---

## 2. 갈래별 증거 현황

### ① 업무 AX — 같은 일을 더 적은 시간으로

**어디서 계산하나** `src/lib/performance.ts` → `/performance` A·B 구역

| 지표 | 출처 | 상태 |
|---|---|---|
| 수거 1건당 행정시간 | 기준값(사람 입력) vs 실제 입력 소요시간 | 기준값 있음 · 실측 표본 부족 |
| 반복입력 감소 | 기준값 vs 1회 입력으로 연결된 단계 수 | 계산 가능 |
| 월 반복업무시간 | 기준값 vs 파생 계산 | 계산 가능 |
| 재입력(되돌리기) | `collection_events` 취소 건수 | 계산 가능 (10건 이상부터) |
| 하루 처리 거래처 | 완료 일정 ÷ 실제 나간 날 | 계산 가능 |
| 자동 연결 건수 | `autoLinkOf(events)` | 계산 가능 |

**당일 입력** (`axEvidence.workAx`) — `/performance` 「업무 AX」 구역

| 지표 | 계산 방법 |
|---|---|
| 당일 입력 완료율 | 다녀온 날(`schedules.date`)과 입력한 날(`completed_at` 한국 시간)이 같은 비율 |
| 다음 날까지 입력한 비율 | 당일 + 다음 날. 당일률과의 차이가 「저녁에 몰아 넣는 습관」의 크기 |
| 며칠 만에 입력했는가 | 분포 (당일 · 1일 뒤 · 3일 뒤 …) |
| 다녀오기 전에 입력된 건 | 있을 수 없는 값. **0 으로 누르지 않고 그대로 셉니다** — 날짜를 잘못 넣은 기록의 표시 |

⚠ `PILOT_7DAY.md` 하루 한 줄 기록의 「당일 입력률」과 **같은 정의**입니다. 두 숫자를 나란히 놓기 위해서입니다.

**아직 못 세는 것**
- **직원별 처리건수** — `capacityAx.byDriver` 로 이미 셉니다(확장 AX 쪽에 있음).
- **카톡/전화/Excel 로 돌아간 이유** — 시스템이 볼 수 없는 자료입니다.
  `PILOT_7DAY.md` 의 하루 한 줄 기록으로 받습니다. **자동화하지 않습니다.**

### ② 매출 AX — 기존 병원에서 새 상품매출

**어디서 계산하나** `src/lib/axEvidence.ts` → `salesAx()` → `/performance` 「매출 AX」 구역

네 단계를 **네 칸으로** 갈라 둡니다.

```
주문 접수 → 전달 완료 → 청구 확정 → 입금 완료
(요청)     (여기부터    (payments      (그 청구가
            매출)        .snapshot       입금 완료)
                         .orderIds)
```

| 지표 | 계산 방법 |
|---|---|
| 소모품 판매매출 | **전달 완료** 주문의 주문시점 판매단가 × 수량 |
| 판매 원가 / 실제 판매이익 | 같은 주문의 주문시점 매입단가 × 수량 / 매출 − 원가 |
| 입금까지 끝난 금액 | 그 주문이 담긴 청구가 **실제 입금 완료**된 것만 |
| 주문 건수 / 구매 병원 수 | 취소 제외 / 전달 완료 주문의 서로 다른 거래처 |
| 재구매 병원 수 | 그 거래처의 **첫 주문이 아닌** 주문 — 한 번은 호의, 두 번째부터가 매출 |
| 병원당 추가매출 | 판매매출 ÷ 구매 병원 수 (0곳이면 계산하지 않음) |
| 수거 청구액 대비 비중 | 소모품 매출 ÷ 같은 기간 확정 수거 청구액 — **두 매출을 한 칸에 더하지 않습니다** |
| 주문 품목 중 추천에 있던 비율 | 주문 하나하나를 **그 주문일 시점으로 되돌려** 추천을 다시 계산 |
| 추천 → 주문 전환율 | 기간을 7일 간격으로 되짚어 한 번이라도 추천이 떠 있던 병원×품목 중 주문된 비율 |

**⚠ 고친 결함 — 추천이 「이미 받아 간 물량」을 못 봤습니다**
`supplyNeedsFor` 는 자재공급(`materials`)만 봤는데, 포털 주문을 전달하면 재고와 원장
(`material_transactions`)에만 남고 `materials` 에는 안 씁니다. 그래서 병원이 박스 20개를 사서 받아도
그다음 주에 또 「마지막 공급 50일 전 · 이번에 필요」라고 권했습니다 — **방금 받은 사람에게 또 사라고**
하는 것입니다. 이제 두 갈래(자재공급 + 전달 완료 주문)를 합쳐서 봅니다.
주문만 하고 못 받은 것 · 취소한 것은 안 셉니다. (`check_needsource` ⑥번이 그 차이를 못 박습니다)

**아직 없는 단계 — 「추천 확인」**
지시문의 흐름에는 *추천됨 → **추천 확인** → 주문 전환* 이 있는데, 「병원이 그 추천을 봤는가」는
지금 어디에도 남지 않습니다. 남기려면 **조회 기록을 저장해야 합니다(새 표 또는 새 칸)** —
대표님 확인이 필요한 사항이라 임의로 만들지 않았습니다.

**추천을 따로 저장하지 않은 이유**
`supplyNeedsFor(data, clientId, 날짜)` 는 「그날까지의 자재 공급 기록」만 보는 **결정적 함수**입니다.
그날 이후 기록을 빼고 다시 돌리면 그날 화면에 떠 있던 추천이 그대로 나옵니다.
→ **새 표가 필요 없습니다.** (`needsAsOf()`)
⚠ 그날 이후 기록을 반드시 빼야 합니다. 안 빼면 「그날은 몰랐던 것」을 알았던 것처럼 셉니다.
`check_axevidence` ② 번이 이걸 지킵니다.

**막고 있는 것 (개발 아님)**
- 실제 **판매가 / 매입가** 미결정 → 상품이 없으니 병원이 주문할 수 없습니다.
- → `PILOT_7DAY.md` A-④ 에서 대표님 결정 대기 중.

### ③ 확장 AX — 같은 기사·차량으로 더 많이

**어디서 계산하나** `src/lib/axEvidence.ts` → `capacityAx()` · `src/lib/routeEfficiency.ts`

| 지표 | 계산 방법 |
|---|---|
| 완료한 방문 / 실제 나간 날 | 완료 일정 (무른 방문 제외) |
| 하루 방문 병원 수 (평균·최대) | 완료 방문 ÷ 실제 나간 날 — **안 나간 날로 나누지 않습니다** |
| 기사별 처리건수 | `schedules.driver_name` — **이름 없는 건을 차량 기본 기사로 채우지 않습니다** |
| 예정에 없던 수거 | 사람이 날짜를 안 잡았는데 현장에서 완료된 건 |
| 관측 최대 대비 여유 | 차량×요일마다 (그 요일 최대 정차 − 평균) |
| **차량 × 요일 표** | 「예전 최대 / 요즘 평균」. 두 번 이상 나간 요일만 적습니다 — 한 번은 「그 요일의 그 차」가 아닙니다 |

「화요일 2호차는 여유가 있는가」에 답하는 자리가 이 표입니다. 답은 **관측된 것**뿐입니다.
| 요일 쏠림 · 같은 날 묶기 | `routeEfficiency.ts` (기존) |

**계산하지 않는 것**
거리(km) · 소요시간 · 최적 경로 순서 — **거래처 좌표가 없습니다.**
「관측 최대 대비 여유」는 「몇 곳 더 받을 수 있다」가 아니라 **「예전에 이만큼은 해냈다」**입니다.
→ 실제 주소·차량 위치 데이터가 충분히 쌓인 뒤에 지도 API 를 검토합니다. **지금 연동하지 않습니다.**

**막고 있는 것** 기사 1명 · 병원 1곳으로는 처리능력을 잴 수 없습니다. 파일럿 확대가 선행입니다.

### ④ 고객 AX — 병원이 직접 쓰기 시작했는가

**어디서 계산하나** `src/lib/axEvidence.ts` → `customerAx()`

| 지표 | 계산 방법 |
|---|---|
| 포털 수거요청 건수 | `client_requests.source = 'portal'` |
| 포털 물품 요청 건수 | `product_orders.source = 'portal'` |
| **전체 요청 중 포털 비율** | 이 값이 오르는 것이 「전화·카톡 대신 쓰기 시작했다」입니다 |
| 포털을 실제로 쓴 병원 수 | 요청이나 주문을 한 번이라도 올린 거래처 |
| 두 번 이상 쓴 병원 수 | 한 번은 시켜서 해 본 것일 수 있습니다 — 두 번째부터가 습관 |
| 주문한 병원 수 | 전달 완료된 주문이 있는 거래처 |

**셀 수 없는 것 (그대로 「셀 수 없음」이라고 표시)**
- 활성 병원 **계정 수** — 계정 목록(`profiles`)은 관리자 화면 자료입니다.
- **로그인 횟수** — 「로그인만 하고 아무것도 안 한 것」은 쓴 것이 아니라고 보고, 세지 않습니다.

---

## 3. 검사 (이 숫자들을 지키는 자)

| 검사 | 무엇을 지키나 | 건수 |
|---|---|---|
| `check_axevidence` | 계산 규칙을 esbuild 로 직접 두들깁니다 — 주문≠매출 · 전달≠입금 · null≠0 · 시점 재현 · 당일 입력 · Before 지어내지 않기 | 58 |
| `check_axui` | 화면이 숫자를 크게 보이게 하지 않는가 — 네 칸 분리 · 「—」 · 「측정 중」 · 변화 칸에 백분율 없음 · 폰 390px | 56 |
| `check_needsource` | 추천이 **실제로 들어간 물량 전부**를 보는가 — 자재공급 + 전달 완료 주문 | 14 |
| `check_clientax` | 거래처별 「지금 아는 것」 — 전달 완료만 셈 · 전달≠입금 · 영업추천 없음 | 18 |

특히 **`check_axevidence` ⑧번**: 900,000원짜리 「주문만 들어온 건」을 fixture 에 넣어 두고,
매출이 135,000원으로 나오는지 봅니다. 주문을 매출로 세는 순간 이 검사가 깨집니다.

---

## 3-2. 거래처별 AX 기회 (`clientAx`)

거래처 상세 「운영조건」 → 「이 병원에 대해 지금 아는 것」.
**이미 일어난 일만** 놓습니다 — 「이 병원에 이걸 파세요」라고 하지 않습니다.
근거 없는 영업추천은 한 번만 틀려도 그다음부터 화면의 모든 숫자를 안 믿게 만듭니다.

자주 가져가시는 물품 · 다음에 필요할 것으로 보이는 것(근거 한 줄) · 병원이 직접 올리는가 ·
예정에 없던 방문·추가요청 · (사무실·관리자만) 소모품 판매액(전달 / 입금 따로).

⚠ 폰에서는 **한 줄로 접힙니다.** 다 펴 두었더니 거래처 상세 폰 길이가 3,273px 이 됐습니다
(한도 3,100px). 이 화면의 「다음 행동 추천」도 폰에서는 같은 방식으로 접혀 있습니다.

---

## 3-3. 성능 — 잰 값 (짐작으로 고치지 마세요)

`needConversion` 은 기간을 7일 간격으로 되짚으며 **모든 거래처 × 모든 시점**에 추천을
다시 계산합니다. 무거워 보여서 실제로 재 봤습니다.

| 자료 | 기간 | axEvidence | 그중 needConversion |
|---|---|---|---|
| 거래처 100곳 · 자재 1,200건 · 일정 5,200건 · 주문 40건 | 최근 90일 | 107ms | — |
| 〃 | 1년 | 223ms | 170ms |

**문제가 아닙니다.** ㈜비원미래 규모(거래처 100곳 이하)에서는 최적화가 필요 없습니다.
`axCompare` 가 앞뒤로 한 번씩 더 부르므로 성과판 한 번 그릴 때 대략 3배(≈0.7초)입니다.
그래도 이 화면은 매일 여는 화면이 아닙니다.

> 나중에 느려지면 **먼저 재고** 고치세요. 이 표를 지우지 말고 새 줄을 더하세요.

---

## 4. 다음 후보 (가치 순)

1. **「추천 확인」 단계** — 병원이 추천을 **봤는지**가 어디에도 안 남습니다.
   남기려면 조회 기록을 저장해야 합니다 → **대표님 확인 사항**. *매출 AX*
2. **성과판의 「목표」 칸** — Before/After 는 닫았습니다. 목표 숫자는 대표님이 정하시는 값입니다. *전체*
3. **파일럿에서 나오는 실제 문제** — 가정한 문제보다 우선입니다.

---

## 5. 파일럿에서 발견된 실제 문제

*(파일럿 시작 전 — 실제 사용에서 나온 문제만 여기에 적습니다. 가정한 문제는 적지 않습니다.)*

지금까지는 **실사용 검증(실제 계정 E2E)** 에서 나온 것들입니다.

| 언제 | 무엇 | 처리 |
|---|---|---|
| 실계정 E2E | 병원 로그인 직후 「연결된 병원 정보를 찾을 수 없습니다」가 몇 초 뜸 (실제론 연결됨) | 고침 — `sync.ready` 도입 |
| 390px 스윕 | 「오늘은 다른 차로 갔어요」 23px — 대차 날 못 누르면 틀린 차량으로 기록 | 고침 — 44px |
| 큰 글씨 스윕 | 수거 입력 용기 이름이 세로로 한 글자씩 (27×238px) | 고침 |
| 지하 보관실 갈래 | 「이미 완료 처리된 일정입니다」가 빨간 실패로 뜸 (실제로는 저장됨) | 고침 — 청록 안내 |

---

## 6. 지금 개발을 더 할 것인가, 데이터를 쌓을 것인가

**데이터를 쌓아야 합니다.**

매출·확장·고객 AX 는 **계산이 다 됩니다.** 넣을 기록이 없을 뿐입니다.
지금 더 만드는 것은 「쓰지 않는 화면을 늘리는 것」이고, 파일럿을 하루 미룰수록
증명은 하루 늦어집니다. 파일럿 3일이면 고객 AX 의 첫 숫자가 나옵니다.

**파일럿을 막고 있는 것은 개발이 아니라 결정입니다** — `PILOT_7DAY.md` A 구역:
기사·담당차량 / 병원·연락처·주소 / 수거요일·시각·시작일 / **판매 소모품 판매가·매입가**.
