# 실운영 계정 전환

검증용으로 쓰던 계정·데이터를 실제 운영 계정으로 바꾸는 절차입니다.
`GO_LIVE.md`(무엇을 언제 입력하는가)와 짝이고, 이 문서는 **계정**만 다룹니다.

---

## 1. 검증 계정과 운영 계정을 구분하는 규칙

섞이지 않게 하는 방법은 하나뿐입니다 — **주소가 겹칠 수 없게** 해 두는 것입니다.

| | 검증·리허설 | 실제 운영 |
|---|---|---|
| 이메일 | `@beonemirae.test` | `@beonemirae.co.kr` (회사 도메인) |
| 거래처·차량 이름 | `[검증]` 으로 시작 | 접두사 없음 |
| 지우는 방법 | `05_live.mjs --cleanup` 이 한 번에 | 지우지 않음 |

`.test` 는 **인터넷에 실제로 존재할 수 없는 도메인**입니다(RFC 2606).
그래서 검증 계정이 실수로 진짜 메일을 받는 일도, 운영 계정과 이름이 겹치는
일도 없습니다. 앞으로 시험용 계정을 더 만들 때도 이 도메인만 쓰세요.

지금 무엇이 남아 있는지는 아무 때나 확인할 수 있습니다 (읽기만 합니다):

```bash
export SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=...
node supabase/test/handover_status.mjs
```

---

## 2. 계정 만드는 순서

**순서가 중요합니다.** 뒤엣것이 앞엣것을 필요로 합니다.

| # | 무엇을 | 왜 이 순서인가 |
|---|---|---|
| 1 | **관리자(대표)** 1명 | 나머지 계정을 만들 사람. 지금은 `[검증]` 관리자로 로그인해서 만듭니다 |
| 2 | **관리자(백업)** 1명 | 관리자가 한 명뿐이면 그 사람이 못 들어올 때 아무도 못 들어옵니다. 시스템이 마지막 관리자의 중지·강등을 막습니다 |
| 3 | **사무실** 계정 | 정산·청구·거래처를 다루는 사람 |
| 4 | **현장** 계정 (기사 수만큼) | 폰으로 수거를 입력하는 사람 |
| 5 | **차량** 등록 | 차량이 없으면 수거 저장 버튼이 잠깁니다 |
| 6 | **거래처(병원)** 등록 | 병원 계정은 소속 거래처가 없으면 만들 수 없습니다 |
| 7 | **병원** 계정 | 6번에서 만든 거래처에 하나씩 연결 |
| 8 | `[검증]` 정리 | **맨 마지막.** 2번이 끝나기 전에는 검증 관리자를 지우면 안 됩니다 |

계정은 모두 **관리자로 로그인 → 「사용자 관리」 → 「계정 만들기」** 에서 만듭니다.
입력하는 것은 이름 / 이메일 / 임시 비밀번호(8자 이상) / 역할, 병원 계정이면
소속 거래처까지입니다.

> **임시 비밀번호**는 화면의 「임의 생성」 버튼을 쓰고, 본인에게 전달한 뒤
> 첫 로그인에서 바꾸게 하세요. 시스템은 비밀번호를 저장하지 않습니다 —
> 감사기록에도 남지 않습니다. 잊어버리면 관리자가 초기화해 주면 됩니다.

---

## 3. 계정을 만들 때마다 하는 확인 (1개당 1분)

만든 사람이 직접, 그 계정으로 한 번씩 해 보세요.

1. **로그인** — 발급한 이메일·임시 비밀번호로 들어가진다
2. **첫 화면** — 역할에 맞는 화면이 뜬다

   | 역할 | 첫 화면 |
   |---|---|
   | 관리자 | 대시보드 |
   | 사무실 | 대시보드 |
   | 현장 | 오늘 일정 |
   | 병원 | 병원 포털 |

3. **권한 제한** — 열리면 안 되는 화면이 막힌다 (주소창에 직접 넣어 확인)

   | 역할 | 막혀야 하는 곳 |
   |---|---|
   | 사무실 | `/users` `/audit` `/settings` `/import` |
   | 현장 | `/receivables` `/stats` `/performance` `/users` `/settings` |
   | 병원 | 포털 밖 전부 — `/clients` `/receivables` `/today` … |

   「접근 권한이 없는 화면입니다」 가 나오면 정상입니다.

4. **로그아웃** — PC 는 왼쪽 아래, **폰은 「더보기」 맨 아래 계정 칸**에 있습니다

네 가지를 사람 대신 확인해 주는 명령도 있습니다. 계정을 하나 만들 때마다
한 번씩 돌리세요. 비밀번호는 셸에만 두고 끝나면 `unset` 하면 됩니다 —
화면에도 로그에도 찍지 않습니다.

```bash
ACCOUNT_EMAIL='hong@회사도메인' ACCOUNT_PW='발급한 임시 비밀번호' \
  node supabase/test/verify_account.mjs
```

역할은 계정에 저장된 값을 그대로 읽어 판단합니다. 관리자면 「사용자 관리 ·
감사로그 · 설정 · 엑셀 가져오기」가 열리는지까지 봅니다. 검증용 도메인
(`@beonemirae.test`)으로 만들어 두면 그것도 짚어 줍니다.

절차 전체가 되는지 다시 확인하고 싶으면 `49_account_handover.mjs` 를 돌리세요 —
리허설 계정을 만들었다가 끝나면 스스로 지웁니다.

```bash
node supabase/test/49_account_handover.mjs
```

---

## 4. 실사용 시작 전에 정리할 것

`handover_status.mjs` 가 현재 상태로 만들어 주는 목록입니다.
**2번의 1~2단계(운영 관리자 2명)를 끝낸 뒤에** 정리하세요.

### 계정 — 5개

| 이메일 | 역할 | 비고 |
|---|---|---|
| `admin@beonemirae.test` | 관리자 | **맨 마지막에 지우세요.** 운영 관리자 2명이 생기기 전에 지우면 아무도 계정을 만들 수 없습니다 |
| `office@beonemirae.test` | 사무실 | |
| `field@beonemirae.test` | 현장 | |
| `client@beonemirae.test` | 병원 | |
| `client2@beonemirae.test` | 현장 | 이름은 `검증병원2` 인데 역할이 현장입니다 — `--cleanup` 이 소속 잃은 병원 계정을 현장으로 되돌린 흔적입니다 |

계정 삭제는 **Supabase 대시보드 → Authentication → Users** 에서 합니다.
앱 화면에는 계정을 지우는 기능이 없습니다(실수로 지우면 되돌릴 수 없어서
일부러 넣지 않았습니다). 당장 지우기가 부담스러우면 「사용자 관리」에서
**중지**만 해 두어도 로그인은 막힙니다.

### 거래처 · 차량 · 딸린 기록

| 대상 | 남은 것 |
|---|---|
| 거래처 | `[검증]한마음요양병원` |
| 차량 | `[검증]검증차량` |
| 수거일정 | 5건 |
| 병원 요청 | 1건 |
| 자재공급 · 청구 · 메모 · 제안 | 0건 |

이 넷은 한 번에 지웁니다. `[검증]` 접두사가 붙은 것만 골라 지우며,
실제 운영 데이터는 건드리지 않습니다 (`47_cleanup_scope.mjs` 가 매 회귀마다
확인합니다).

```bash
node supabase/test/05_live.mjs --cleanup
```

> `--cleanup` 은 **계정을 지우지 않습니다.** 거래처·차량·기록만 지우고,
> 소속 병원이 사라진 병원 계정은 역할을 `field` 로 되돌립니다.
> 계정은 위의 표대로 대시보드에서 따로 지우세요.

### 정리한 뒤

```bash
node supabase/test/handover_status.mjs
```

**「정리할 것 없음 — 실사용을 시작해도 됩니다」** 가 나오면 끝입니다.

---

## 5. 막히기 쉬운 곳

**관리자가 한 명뿐일 때** — 그 사람은 자기 계정을 중지할 수도, 자기 관리자
권한을 내릴 수도 없습니다. 화면과 서버 양쪽이 막습니다. 잠기지 않게 하려고
일부러 그렇게 해 두었습니다. 관리자를 바꾸려면 새 관리자를 먼저 만드세요.

**병원 계정을 먼저 만들려고 할 때** — 거래처가 없으면 만들 수 없습니다.
「거래처」 화면에서 병원을 먼저 등록하세요.

**계정을 중지하면 언제부터 막히나** — 즉시입니다. 쓰고 있던 사람도 다음
화면부터 「비활성화된 계정입니다」 로 막히고, 거기서 로그아웃할 수 있습니다.

**다른 기기에서 로그아웃시키고 싶을 때** — 「모든 기기에서 로그아웃」을 해도
이미 발급된 접근 토큰은 만료될 때까지(기본 1시간) 살아 있습니다. Supabase
토큰 방식이라 앱에서 막을 수 없습니다. **급하면 계정을 중지하세요 — 그건
즉시 반영됩니다.** 만료 시간을 줄이려면 Supabase 대시보드에서 JWT expiry 를
낮추면 됩니다.

**계정을 얻는 길은 둘입니다.** 관리자가 「사용자 관리 → 계정 만들기」로
직접 만들거나, 본인이 「가입 신청」을 하고 관리자가 승인하는 것입니다.

가입 신청은 **신청만으로는 아무 힘이 없습니다.** 스스로 관리자라고 적어
보내도 서버는 그 값을 읽지 않고 현장 담당자 + 승인 대기로 만듭니다. 승인
전까지 거래처·수거·청구·감사기록이 전부 막혀 있습니다 — 화면에서 가리는 게
아니라 RLS 가 막습니다. 역할은 승인하는 관리자가 그 자리에서 정합니다.
(`0021_signup_approval.sql` · `04_signup_approval.sql` · `11_auth_boundary.mjs`)

승인 대기는 「사용자 관리」 맨 위에 뜹니다. 신청이 들어온 줄 모르고 지나가면
신청한 사람은 로그인만 하면 되는 줄 알고 기다립니다.

**관리자는 두 계정입니다** — `sanghohoho0813@gmail.com` ·
`beonemirae@naver.com`. 0021 이 이 둘을 관리자로 두고, 그 외 관리자는 사무실
담당자로 내립니다. 바꾸시려면 「사용자 관리」에서 역할을 바꾸면 됩니다
(마지막 관리자는 스스로도 남도 내릴 수 없습니다 — 0016).
