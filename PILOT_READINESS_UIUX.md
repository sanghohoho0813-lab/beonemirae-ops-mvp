# 실증 전 UI/UX · 역할별 사용성 마감

> 이 문서는 **계속 갱신하는 작업 상태판**입니다.
> 새 세션은 이 문서를 먼저 읽고, 아래 「다음 우선순위」부터 이어서 진행합니다.
>
> 이 단계의 목표는 화면을 예쁘게 만드는 것이 아니라
> **대표·직원·병원 관계자가 설명 없이 실제로 쓸 수 있게** 만드는 것입니다.
> 신규 사업기능 개발은 **Freeze** 상태입니다.

---

## 종료 조건

| 대상 | 조건 |
|---|---|
| 대표·관리자 | 로그인 후 **30초 안에** 오늘 확인·처리할 것을 안다 |
| 현장직원 | 폰으로 설명 없이 **오늘 일정 → 방문 → 수거입력 → 자재입력 → 완료** 까지 끝낸다 |
| 병원 관계자 | 로그인 후 고민 없이 **① 수거 요청 ② 소모품 주문** 중 원하는 쪽으로 바로 간다 |

---

## Phase 진행 상태

| Phase | 내용 | 상태 |
|---|---|---|
| **A** | 전 역할·전 화면 UI/UX 감사 | **완료 (2026-08-18)** |
| **B** | 현장직원 실제 모바일 사용 정상화 — **터치 대상** | **완료 (2026-08-18)** · 실계정 미검증 |
| **C** | 병원 포털 — 수거요청 + 소모품주문 중심 UX | **완료 (2026-08-18)** · 실계정 미검증 |
| **D** | 관리자 정보위계·레이아웃 정리 | **완료 (2026-08-18)** · 실계정 미검증 |
| **E** | 나머지 모바일 responsive 정리 | **부분 완료** — 남은 것은 M5·M6·M7 |
| F | 4개 역할 실제 계정 E2E + 최종 회귀 | **대기 (대표님 계정 결정 필요)** |

---

## Phase A — 감사 방법과 범위

`scratchpad/audit_ux.mjs` 로 **120화면**을 측정했습니다.

- 역할 4개: `admin` · `office` · `field` · `client`
- 해상도 2개: **PC 1440×900** · **모바일 390×844** (`isMobile` + `hasTouch`)
- 화면: 각 역할이 실제로 열 수 있는 경로 전체 (`src/lib/access.ts` 정책 그대로)
- `/mobile-preview` 로 판단하지 않았습니다. 각 역할 토큰으로 실제 앱을 띄웠습니다.

### 무엇을 쟀는가 (눈이 아니라 숫자)

| 항목 | 판정 기준 |
|---|---|
| 가로 넘침 | `documentElement.scrollWidth − clientWidth` 와 **화면 밖으로 나간 요소** |
| 넓은 표 | `<table>` 폭 > 화면 폭 |
| 작은 글자 | 모바일에서 계산된 `font-size` < 13px |
| 작은 터치 대상 | 모바일에서 버튼·링크·입력 높이 < 36px |
| 하단 내비 가림 | 문서 맨 아래로 내린 뒤 **고정 내비에 걸치는 조작 요소** |
| 세로로 늘어진 글자 | 글자 두 자 폭보다 좁은데 세 줄보다 높은 칸 |
| 빈 상태 표시 | 「없습니다 / 미설정 / 미측정 / 측정 중 …」 등장 횟수 |
| 페이지 길이 | 문서 높이 ÷ 화면 높이 = **몇 화면을 밀어야 하는가** |

### 이 감사 도구가 스스로를 검사하는 규칙

측정 도중 **세 번 「이상 없음」이 거짓으로 나왔습니다.** 그래서 판정을 추가했습니다.

1. **로그인 화면을 잰 것을 통과로 세지 않는다** — 첫 실행에서 admin 56화면이 전부 로그인 화면이었는데 「넘침 0 · 문제 0」으로 깨끗하게 나왔습니다.
   원인은 하니스였습니다: 프로필 기본키 조회(`id=eq.<uid>`)에 2행 이상을 돌려줬고, **앱이 이를 올바르게 거부**했습니다.
2. **오류 화면(ErrorBoundary)을 잰 것을 통과로 세지 않는다** — fixture 컬럼명이 실제 스키마와 달라(`billing_month`→`month`, 메모 `content`→`body`, 제품 `sale_price`→`unit_price`, 입금 `payment_receipts`에 청구 행) 화면이 오류 상태인데 측정값은 깨끗했습니다.
3. **fixture 는 DB 컬럼 이름 그대로 쓴다** — `repo.ts` 의 매핑을 한 칸씩 대조해 다시 만들었습니다.

> ⚠ 위 3건은 전부 **테스트 하니스 결함**이며 제품 결함이 아닙니다.
> 다만 「화면이 깨져 있는데 검사에서 이상 없음이 나오는」 상태였으므로 그대로 기록해 둡니다.

---

## Phase A — 결과 요약

### ✅ 전 화면 통과 (120/120)

| 완료 기준 | 결과 |
|---|---|
| 의미 없는 수평스크롤 | **0** (PC·모바일 전 화면) |
| 화면 밖으로 나간 요소 | **0** |
| 하단 내비에 가려지는 핵심 조작 | **0** |
| 화면이 터진 곳 | **0** |
| 권한 없는 화면 노출 | **0** (역할별 경로 정책대로) |

가로 스크롤과 하단 내비 겹침은 **이미 해결되어 있습니다.** 영상에서 우려하신 항목 중 이 둘은 재현되지 않았습니다.

### 남은 문제 (HIGH / MEDIUM / LOW)

#### 🔴 HIGH — 업무가 막히거나 목표를 정면으로 어기는 것

| # | 화면 | 문제 | 근거 |
|---|---|---|---|
| ~~H1~~ | ~~**병원 포털 홈** (모바일)~~ | **해소 (Phase C).** 첫 화면에 [수거 요청] · [소모품 주문] 두 CTA. 소개 카드는 그 아래로 | 재측정 스크린샷 |
| ~~H2~~ | ~~**오늘 일정** (전 역할, 모바일)~~ | **해소 (Phase B).** 36px 미만 터치 대상 **8 → 0**. 원인은 달력 날짜 칸이 67px 인데 눌리는 버튼은 27px 뿐이었던 것 | 재측정값 |
| ~~H3~~ | ~~**설정** (모바일)~~ | **해소 (Phase D).** 10,457px → **4,883px** (12.4화면 → 5.8화면) | 재측정값 |

#### 🟠 MEDIUM — 쓸 수는 있으나 찾기 어렵거나 반복동작이 많은 것

| # | 화면 | 문제 | 근거 |
|---|---|---|---|
| ~~M1~~ | ~~**사용자 관리** (모바일)~~ | **해소 (Phase D).** 폰에서는 이름·역할·사용여부만 줄에 두고, 「바꾸기」를 누를 때 조작부가 펼쳐집니다. PC 는 그대로 | 재측정값 |
| M2 | **월말 청구** (모바일) | 4,885px = **5.8화면** | 측정값 |
| M3△ | **거래처 상세** (모바일) | 3,745 → **3,569px.** 탭이 폰에서 **화면 위에 붙어(sticky)** 스크롤해도 손에 닿습니다. 길이 자체는 아직 4.2화면 | 재측정값 |
| ~~M4~~ | ~~**매출 현황** (모바일)~~ | **해소 (Phase D).** 12개월 막대의 누르는 자리가 제 내용 높이(34px)뿐이었습니다 — 기둥 전체(160px)로 넓혔습니다. **8 → 0** | 재측정값 |
| M5 | **통계 · AX 성과 · 수거이력 · 병원 수거내역** | 표가 화면보다 넓음(605~1,445px). 컨테이너 안 가로스크롤이라 **의도적 허용 범위**지만, 모바일에서 카드형으로 바꿀 후보 | 측정값 4화면 |
| M6△ | **병원 월 리포트 해소 · 미수금은 재현 못 함** | **병원 월 리포트: 해소** — 병원 월 리포트 제목이 71px 폭 · 165px 높이로 세로로 늘어져 있었습니다. 옆의 달 고르기가 `shrink-0` 이라 자리를 다 가져가고 제목은 `flex-1 min-w-0` 이라 줄바꿈 대신 0 까지 줄어들었습니다 — flex-wrap 이 있어도 제목이 0 까지 줄 수 있으면 줄이 안 넘어갑니다. 폰에서 제목이 한 줄을 통째로 쓰게(`basis-full sm:basis-0`) | 재측정 0건. ⚠ **미수금 화면은 이번에 재현하지 못했습니다** — 청구 자료를 넣어 폰 390px 로 다시 재 봤지만 세로로 늘어진 글자가 안 나왔습니다. 못 고친 것을 고쳤다고 적지 않습니다. 실제 청구 자료가 쌓이면 다시 봅니다 |
| M7 | **통계 · 설정** | 빈 상태 표시가 한 화면에 **8개** — 실증 초기라 「미설정/측정 중」이 반복되어 미완성처럼 보임 | 측정값 |
| ~~M8~~ | ~~**대시보드** (모바일)~~ | **해소 (Phase D).** 「오늘 처리할 업무」가 폰 y=1,189→**800px**, PC y=867→**639px** — 둘 다 첫 화면 안 | 재측정값 |

#### 🟡 LOW — 미관·표현

| # | 내용 |
|---|---|
| L1 | 모바일 60화면 중 55화면에 13px 미만 글자 — 전부 **헤더의 `BEONEMIRAE` 로고와 태그라인(10px)** 이며 본문이 아닙니다. 본문 가독성 문제는 아니지만 통일 대상 |
| ~~L2~~ | ~~병원 포털 헤더의 병원명이 「의료법인 한…」으로 잘림~~ **해소** — 어느 병원인지는 **뒤쪽**에 있습니다(「의료법인 한마음의료재단 **한마음요양병원**」). 두 줄까지 허용(`line-clamp-2`), 헤더 80→92px. 「의료법인」 같은 앞머리를 임의로 떼지는 않습니다 — 등록된 이름을 저희가 줄여 부를 일이 아닙니다 |
| L3 | 병원 포털 본문 하단 여백이 `pb-[40vh]` — **일부러 그런 것으로 확인**. 폰 하단 탭(58px)을 비우는 것 말고도, 사용 방법 투어가 **마지막 자리를 화면 가운데로 끌어올릴** 여백이 필요합니다. 줄이면 투어가 깨지므로 그대로 둡니다 |

---

## 화면 목록 · 역할별 상태

> 상태 표기: ✅ 문제 없음 · ⚠ MEDIUM · ⛔ HIGH · ▫ 미검증(실계정)

| 화면 | 경로 | admin | office | field | client | PC | 모바일 |
|---|---|---|---|---|---|---|---|
| 대시보드 | `/` | ✅ | ✅ | — | — | ✅ | ✅ |
| 오늘 일정 | `/today` | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| 수거 입력 | `/collection` | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| 거래처 목록 | `/clients` | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| 거래처 상세 | `/clients/:id` | ✅ | ✅ | ✅ | — | ✅ | ⚠ M3 (길이만 남음) |
| 병원 요청 | `/requests` | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| 배차·경로 | `/dispatch` | ✅ | ✅ | — | — | ✅ | ✅ |
| 일정 편성 | `/plan` | ✅ | ✅ | — | — | ✅ | ✅ |
| 수거이력 | `/history` | ⚠ M5 | ⚠ M5 | — | — | ✅ | ⚠ |
| 자재 관리 | `/materials` | ✅ | ✅ | — | — | ✅ | ✅ |
| 미수금 | `/receivables` | ⚠ M6 재현 못 함 | ⚠ M6 재현 못 함 | — | — | ✅ | ⚠ |
| 월말 청구 | `/billing` | ⚠ M2 | ⚠ M2 | — | — | ✅ | ⚠ |
| 단가 점검 | `/pricing` | ✅ | ✅ | — | — | ✅ | ✅ |
| 매출 현황 | `/revenue` | ✅ | ✅ | — | — | ✅ | ✅ |
| 소모품 주문 | `/supplies` | ✅ | ✅ | — | — | ✅ | ✅ |
| 통장 대사 | `/bank` | ✅ | ✅ | — | — | ✅ | ✅ |
| 통계 | `/stats` | ⚠ M5 M7 | ⚠ M5 M7 | — | — | ✅ | ⚠ |
| 보고서 | `/reports` | ✅ | ✅ | — | — | ✅ | ✅ |
| AX 성과 | `/performance` | ⚠ M5 | ⚠ M5 | — | — | ✅ | ⚠ |
| 더보기 | `/more` | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| 설정 | `/settings` | ✅ | — | — | — | ✅ | ✅ |
| 사용자 관리 | `/users` | ✅ | — | — | — | ✅ | ✅ |
| 변경 이력 | `/audit` | ✅ | — | — | — | ✅ | ✅ |
| 요청함 | `/dev-requests` | ✅ | — | — | — | ✅ | ✅ |
| 엑셀 가져오기 | `/import` | ✅ | — | — | — | ✅ | ✅ |
| 만든 이유 | `/why` | ✅ | ✅ | — | — | ✅ | ✅ |
| 활용 계획 | `/roadmap` | ✅ | ✅ | — | — | ✅ | ✅ |
| 포털 홈 | `/portal` | ✅ | — | — | ✅ | ✅ | ✅ |
| 포털 소모품 | `/portal/supplies` | — | — | — | ✅ | ✅ | ✅ |
| 포털 수거내역 | `/portal/history` | — | — | — | ⚠ M5 | ✅ | ⚠ |
| 포털 리포트 | `/portal/report` | — | — | — | ✅ | ✅ | ✅ |

**PC 1440 은 31개 화면 전부 통과했습니다.** 남은 문제는 사실상 전부 모바일과 정보위계입니다.

---

## 준비도

| 항목 | 현재 | 근거 |
|---|---|---|
| **현장직원 모바일 준비도** | **75%** | 넘침·가림 0 · **터치 대상 0개 남음(Phase B 완료)** · 실계정 미검증 |
| **병원 포털 준비도** | **75%** | 첫 화면에 두 CTA · 소모품이 **실제 주문 화면**으로 연결 · 터치 대상 정리됨 · 실계정 미검증 |
| **관리자 UI 준비도** | **88%** | PC 전 화면 통과 · 오늘 할 일이 첫 화면 안 · 설정 5.8화면 · 사용자 관리 카드 정리 |
| **실계정 검증** | **0%** | 아래 「막힌 것」 참조 |
| **Pilot Readiness (종합)** | **80%** | A 58% → B 66% → C 72% → D 80% |

---

## 🔒 관리자(개발자) 테스트 데이터 격리 — 지금은 **안 되어 있습니다**

대표님 질문: 「관리자에서 테스트 삼아 입력하는 정보가 대표·이사 화면에
적용되지 않게 해줘. 지금도 그렇게 돼있나?」

**답: 아니오. 지금은 그대로 섞여 보입니다.**

- RLS 는 **역할(admin/office/field/client) 기준**입니다. 사람 기준이 아닙니다.
- 대표님과 개발자가 **둘 다 `admin`** 이면 서로의 입력이 구분되지 않습니다.
- `demo_session_id` 가 있긴 하지만 그건 **브라우저 안에서 도는 시연 모드**를
  되돌리는 도구이고(`lib/demo.ts`), 운영 DB 의 사용자별 격리가 아닙니다.

즉 관리자 계정으로 운영 DB 에 넣은 수거·청구·거래처는 대표님·이사님 화면의
매출·정산·미수금에 **그대로 반영됩니다.** 돈이 틀릴 수 있는 문제라 임의로
바꾸지 않고 여쭙니다.

### 고르실 수 있는 세 가지

| | 방법 | 좋은 점 | 나쁜 점 |
|---|---|---|---|
| **A** | **테스트용 Supabase 프로젝트를 따로** 만들고, 개발자는 거기만 씁니다 | 운영 DB 에 **손이 아예 안 닿습니다.** 가장 깨끗하고, 실수로 지울 위험도 0 | 프로젝트 하나가 더 생깁니다(무료 등급으로 가능). 마이그레이션을 두 번 실행 |
| **B** | 운영 DB 에 **「테스트 자료」 표시**를 달고, 대표·이사 화면에서 걸러 냅니다 | 프로젝트 하나로 끝 | 표 전부에 칸을 더하고 **모든 집계를 고쳐야** 합니다. 한 군데라도 빠뜨리면 매출이 틀립니다 |
| **C** | 개발자 계정을 **읽기 전용**으로 두고, 입력은 안 합니다 | 지금 구조 그대로, 오늘 바로 됨 | 입력 흐름을 테스트할 수 없습니다 |

**제 의견은 A 입니다.** B 는 33개 표와 모든 집계에 손을 대야 해서, 이 시스템이
지켜 온 「돈이 틀리지 않는다」를 가장 크게 흔듭니다. A 는 오늘 하루면 됩니다 —
새 프로젝트를 만들고 마이그레이션 0001~0062 를 그대로 한 번 돌린 뒤,
개발자 브라우저의 접속 정보만 그쪽으로 바꾸면 끝입니다.

> 어느 쪽으로 할지 정해 주시면 그때 진행하겠습니다. RLS·아키텍처 변경은
> 여쭤보기로 한 항목입니다.

---

## 막힌 것 — 대표님 결정이 필요합니다

실계정 E2E(Phase F)를 하려면 계정이 필요한데, 여기서는 조회할 수 없습니다.

- 운영 Supabase에 anon 키로 `profiles` 조회 → `HTTP 401 permission denied` (**권한이 정상적으로 잠겨 있습니다**)
- `service_role` 키는 요청하지 않습니다 — 승인 필요 항목이고, 프런트에 두지 않기로 한 값입니다

계정 생성 경로는 이미 있습니다: **관리자 로그인 → 사용자 관리 → 계정 만들기** (`admin_create_user` RPC).

**여쭐 것 세 가지**
1. 지금 만들어 두신 계정이 역할별로 무엇무엇인지 (admin / office / field / client)
2. 검증용 계정 4개를 새로 만드실지, 기존 것을 쓸지
3. client 계정은 어느 병원에 연결할지

> 비밀번호는 소스·픽스처·git·문서 어디에도 적지 않습니다.

---

## 다음 우선순위

### 다음 회차 (Phase E 마무리)

1. **M7 — 빈 상태 표현 통일.** 실증 초기라 「미설정 / 측정 중 / 없음」이 한
   화면에 8개까지 반복됩니다. 「무엇을 하면 채워지는지」로 바꿉니다.
   **없는 성과를 만들어 보여주지는 않습니다.**
2. **M5 — 넓은 표 4개**(통계 · AX 성과 · 수거이력 · 병원 수거내역)를
   폰에서 카드형으로. 지금은 컨테이너 안 가로스크롤(의도적 허용).
3. **M2 — 월말 청구 5.8화면** · **M3 — 거래처 상세 4.2화면** 길이 줄이기

### 그 다음

4. ~~M6 세로로 늘어진 글자 2건~~ → 병원 월 리포트 해소 · **미수금은 재현 못 함**
5. L1 헤더 로고 글자 크기 (~~L2 병원명 잘림 해소~~ · ~~L3 포털 하단 여백은 일부러 그런 것으로 확인~~)
6. **Phase F — 4개 역할 실계정 E2E** (대표님 계정 결정 후)

---

## Phase D — 관리자 정보위계 (완료)

「대표가 로그인하면 30초 안에 오늘 할 일을 안다」를 **첫 화면에 무엇이
보이는가**로 바꿔서 쟀습니다.

### ① 대시보드 — 오늘 할 일이 첫 화면 밖에 있었습니다

| | Before | After |
|---|---:|---:|
| 「오늘 처리할 업무」 위치 (PC 1440×900) | y=**867px** (첫 화면 밖) | y=**639px** |
| 「오늘 처리할 업무」 위치 (폰 390×844) | y=**1,189px** (1.4화면 아래) | y=**800px** |
| 마감 배너 높이 (폰) | **778px** | **389px** |
| 페이지 전체 (폰) | 3,278px | **2,889px** |

범인은 **마감 배너 하나**였습니다. 밀린 항목 세 건을 전부 펼치고 근거 설명
세 줄까지 늘 띄워서, 이 띠 하나가 폰 화면 하나에 육박했습니다.
급한 순서로 정렬되어 있으므로 **맨 위 한 건만 펼치고** 나머지는 원래 있던
「나머지 N건 보기」에 둡니다. 근거 설명은 펼쳤을 때 함께 보입니다.

### ② 설정 — 12.4화면을 밀어야 했습니다

| | Before | After |
|---|---:|---:|
| 폰 | **10,457px** (12.4화면) | **4,883px** (5.8화면) |
| PC | 6,933px | 6,933px (그대로) |

설정은 「무엇이 있는지 훑고 하나를 고르는」 화면입니다. 폰에서는 카드 제목만
늘어놓고 **누른 것만 펼칩니다.** 태블릿·PC 는 자리가 넉넉해 그대로 다 펼칩니다.
`/settings#vehicles` 처럼 바로가기로 들어오면 그 카드는 폰에서도 펼쳐집니다.
가장 컸던 「AX 실증 · 도입 전 기준값」(폰 1,948px)도 같은 방식으로 접었습니다.

### ③ 사용자 관리 — 계정 한 줄이 세로로 늘어졌습니다

역할 칩 3개 + 사용상태 + 비밀번호 초기화 + 담당 차량 고르개가 각각 줄바꿈돼
계정 하나가 380px 였습니다. 폰에서는 **이름 · 역할 · 사용여부**만 줄에 두고,
「바꾸기」를 누르면 조작부가 펼쳐집니다. PC 는 그대로입니다.

### ④ 매출 현황 — 막대를 눌러도 안 눌렸습니다

12개월 막대는 160px 짜리 기둥인데 **누르는 자리는 제 내용 높이(34px)**
뿐이었습니다. 막대 위쪽 빈 곳을 눌러도 아무 일이 없었습니다.
기둥 전체가 눌리게 했습니다 — 36px 미만 터치 대상 **8 → 0**.

### ⑤ 거래처 상세 — 탭이 세 화면 아래

폰에서 탭 줄이 y=2,582px 에 있었습니다. 위치를 옮기면 화면 구조가 바뀌므로
대신 **화면 위에 붙였습니다(sticky)** — 한 번 지나가면 계속 손에 닿아, 탭을
옮길 때마다 위로 되돌아가지 않아도 됩니다. 추천 안의 「영업 진행」(폰 239px)은
눌러서 펼치게 해 3,745 → 3,569px. **길이 자체는 아직 4.2화면으로 남습니다.**

### 바꾼 파일

`DeadlineBanner.tsx` · `Settings.tsx` · `UserAdmin.tsx` · `Revenue.tsx` ·
`ClientDetail.tsx`. 업무로직·계산식·권한·DB 는 건드리지 않았습니다.

### 회귀 — 실패 2건이 났고, 왜 고쳤는지

마감 띠를 3건 → 1건으로 바꾸자 `check_deadlineui` 두 건이 실패했습니다.

**기대값을 시스템에 맞춘 것이 아닙니다.** 그 검사가 지키려던 뜻은
「화면을 다 먹지 않게」였고, 이번 변경은 그 뜻을 **더 잘** 지킵니다.
그래서 숫자를 갱신하면서 **실제 높이(420px 이하)를 재는 검사를 새로 넣어**
다음에 또 늘어나면 잡히게 했습니다. 「두 단계를 다 짚는지」는 펼친 뒤에
확인하도록 바꿔, 펼치기가 실제로 되는지도 함께 검사됩니다.

최종 — `tsc` 통과 · 빌드 통과 ·
**스위트 75개 · 검사 2,098건 · 실패 0 · 깨진 스위트 0.** SQL 무변경.

---

## Phase B — 현장직원 모바일 터치 대상 (완료)

현장직원이 장갑 끼고 한 손으로 누르는 자리를 **손가락 크기**로 맞췄습니다.
업무로직·계산식·권한·DB는 한 줄도 건드리지 않았습니다. 크기와 여백만 바꿨습니다.

### 가장 컸던 것 — 달력 날짜 칸이 안 눌리던 문제

`ScheduleCalendar.tsx` 의 날짜 칸은 **67px** 인데, 실제로 누르는 버튼은
글자 높이 **27px** 뿐이었습니다. 칸의 아래쪽 40px 을 눌러도 아무 일도
일어나지 않았습니다. 칸을 `flex flex-col`, 버튼을 `flex-1` 로 바꿔
**칸 전체가 눌리게** 했습니다. 보이는 모양은 그대로입니다.

같은 칸의 ＋(방문 잡기) 는 36px 로만 키웠습니다. 44px 로 키웠더니
폰에서 칸 하나가 56px 밖에 안 되어 **＋ 가 날짜 고르기를 가로챘습니다.**

### Before → After (측정값, 모바일 390)

| 화면 | 36px 미만 터치 대상 | |
|---|---|---|
| 오늘 일정 (field) | **8 → 0** | 달력 날짜 6칸 · 「오늘로 이동」 |
| 오늘 일정 (admin·office) | **8 → 0** | 위 + 마감 배너 링크 2개 |
| 거래처 상세 (admin) | **5 → 0** | 뒤로 · 수정 · 거래 종료 · 삭제 · 방문 더 잡기 |
| 거래처 상세 (field) | **1 → 0** | 뒤로 |
| 병원 요청 (field) | **3 → 0** | 회신 남기기 · 거래처 링크 2개 |
| 대시보드 (admin) | **2 → 0** | 마감 배너 링크 |
| 병원 포털 홈 | **3 → 0** | 상단 사용방법·전화·계정 |
| 병원 소모품 주문 | **4 → 1** | 남은 1개는 아래 참조 |
| 수거 입력 (field) | 0 → 0 | 원래 문제 없었음 |

남은 1개는 「수거 때 함께 받기」 **체크박스(22px)** 입니다. 기본
체크박스라 13px → 20px 으로 키웠고, 이 체크박스는 `p-3.5` 짜리 큰
`<label>` 안에 있어 **실제로 누르는 면적은 약 60px** 입니다. 체크박스만
44px 로 키우면 모양이 어색해져 여기서 멈췄습니다.

### 바꾼 파일

| 파일 | 무엇을 |
|---|---|
| `src/components/ScheduleCalendar.tsx` | 날짜 칸 전체가 눌리게 · ＋ 36px |
| `src/pages/TodaySchedule.tsx` | 「오늘로 이동」 44px |
| `src/pages/ClientDetail.tsx` | 뒤로 · 수정 · 거래 종료 · 삭제 · 방문 잡기 44px |
| `src/pages/Requests.tsx` | 회신 남기기 · 거래처 링크 44px |
| `src/components/DeadlineBanner.tsx` | 마감 배너 링크 44px |
| `src/components/PortalLayout.tsx` | 상단 버튼 3개 44px |
| `src/pages/PortalSupplies.tsx` | 체크박스 13px → 20px |

### 회귀

`tsc --noEmit` 통과 · 빌드 통과 ·
**브라우저 스위트 75개 · 검사 2,097건 · 실패 0 · 깨진 스위트 0.**
DB·SQL 은 건드리지 않아 격리 DB 스위트는 그대로입니다(35개 · 1,190건).

---

## Phase C — 병원 포털 첫 화면 (완료)

### 문제는 두 개였습니다

**① 소개 카드가 첫 화면을 다 차지했습니다.**
폰에서 화면을 열면 「비원미래 AX 운영시스템」 안내 카드가 보이는 영역을 거의
다 덮었고, 「긴급 수거 요청」은 화면 맨 끝에 겨우 걸쳤으며 **「소모품」은 아예
보이지 않았습니다.** 병원 담당자는 폐기물이 본업이 아닌 겸직인 경우가 많아,
안 보이면 그냥 전화를 겁니다.

**② 「소모품」 버튼이 실제 주문으로 이어지지 않았습니다 — 더 중요한 문제입니다.**
큰 버튼의 「소모품」이 **자유 글 요청**으로 갔습니다. 병원이 「20L 용기 10개요」
라고 적으면 그건 요청 한 줄(`client_requests`)로만 남고, 품목·수량·단가가 붙은
**주문(`product_orders`)** 이 되지 않습니다. 그러면 전달해도 **그 달 청구에
안 실리고 소모품 매출로 잡히지 않습니다.**

8월 실판매를 만들려면 목록에서 품목과 수량을 고른 주문이어야 합니다.

### 무엇을 바꿨나

| | Before | After |
|---|---|---|
| 첫 화면 맨 위 | 시스템 소개 카드 | **[수거 요청] · [소모품 주문]** 큰 버튼 두 개 |
| 「소모품」 누르면 | 자유 글 요청 창 | **소모품 주문 화면**(`/portal/supplies`) — 추천·품목·수량·단가 |
| 작은 버튼 | 추가 수거 · 교육·자료 | **긴급 수거** · 교육·자료 |
| 소개 카드 | 맨 위 | 행동 버튼 **아래** (「오늘 하루 보지 않기」 그대로) |

**아무것도 없애지 않았습니다.** 자유 글로 적고 싶은 분을 위해 요청 창의
「소모품」 항목은 그대로 있고, 「긴급 수거」는 작은 버튼으로 남았습니다.
긴급 요청은 예전처럼 「긴급합니다」가 미리 켜진 채로 열립니다 — 큰 버튼의
「수거 요청」은 중립(추가수거·긴급 꺼짐)으로 열려, **모르고 긴급으로 보내는
일이 없게** 했습니다.

### 바꾼 파일

`src/pages/PortalHome.tsx` 한 개. 업무로직·계산식·권한·DB는 건드리지 않았습니다.

---

## 지키는 원칙

- **업무로직·계산식·RLS·역할권한·DB는 건드리지 않습니다.** UI 배치만 바꿉니다.
- 없는 성과를 만들어 보여주지 않습니다. 빈 상태는 **「무엇을 하면 채워지는지」**로 적습니다.
- navy / blue / white 정체성은 유지합니다.
- 한 회차에 1~3개 영역만 끝냅니다.
- **실제로 검증하지 않은 화면은 「완료」라고 쓰지 않습니다.**

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-18 | Phase D 완료 — 대시보드 「오늘 할 일」이 첫 화면 안으로(PC 867→639px · 폰 1,189→800px). 설정 12.4→5.8화면. 사용자 관리·매출 막대·거래처 상세 정리 |
| 2026-08-18 | Phase C 완료 — 병원 포털 첫 화면에 [수거 요청]·[소모품 주문]. 「소모품」이 자유 글 요청이 아니라 **실제 주문 화면**으로 연결 |
| 2026-08-18 | Phase B 완료 — 폰 터치 대상 정리. 달력 날짜 칸이 27px 만 눌리던 문제 해소. 회귀 75스위트·2,097검사·실패 0 |
| 2026-08-18 | Phase A 완료 — 120화면 측정. HIGH 3 · MEDIUM 8 · LOW 3. 감사 도구에 「로그인 화면·오류 화면을 통과로 세지 않는」 판정 추가 |
