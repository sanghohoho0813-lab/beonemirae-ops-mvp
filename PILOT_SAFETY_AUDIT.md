# 파일럿 전 최종 안전점검 — 결과

> **점검일** 2026-08-20 (KST) · **원칙** 검사만 하고 **아무것도 고치지 않았습니다.**
> 운영 DB에 한 줄도 쓰지 않았습니다. 발견한 것은 아래에 등급으로 정리했습니다.

---

## 무엇으로 확인했나 — 「화면」이 아니라 「서버」

이 점검의 핵심은 **실제 계정의 진짜 access token으로 서버를 직접 두들긴 것**입니다.
화면에서 안 보이는 것과 서버가 안 주는 것은 다릅니다. 화면은 언제든 바뀔 수 있고,
토큰만 있으면 화면을 거치지 않고 바로 물어볼 수 있습니다.

| 방법 | 무엇을 봤나 |
|---|---|
| 실계정 REST/RPC 직접 호출 | 현장 28건 · 병원 29건 · 로그인/세션 8건 |
| 실계정 모바일 390px E2E | 현장 하루 흐름 · 병원 요청/주문 · URL 가드 13건 × 2역할 |
| 격리 DB (운영과 같은 migration) | 돈·수량 중복 268건 · 흐름 연결 164건 |
| 화면 자 (390px · 1440px) | 26화면 × 2크기 = 239건 + PC 107건 |

---

## 발견한 것

### 🔴 HIGH 1건 — 병원이 우리 **매입원가**를 읽습니다

병원 계정(`theone@beonemirae.com`)의 실제 토큰으로 확인:

```
GET /rest/v1/products?select=id,name,cost_price
→ HTTP 200 · [{"name":"니트릴 검진장갑","cost_price":0}, …]
```

**지금은 매입가가 전부 0원이라 무해합니다.** 그런데 파일럿 준비로 매입가를 넣는
**바로 그 순간** 병원이 우리 마진을 봅니다 — 「9,000원에 파는데 5,200원에 사 오는구나」.

- **왜 이렇게 됐나** 0063이 거래처 돈 칸을 닫을 때, 상품은 병원도 **목록을 봐야 해서**
  표 전체를 닫을 수 없었습니다. 행(RLS)은 맞게 열었는데 **칸(원가)은 같이 열렸습니다.**
- **수정안** `supabase/proposals/PROPOSAL_0064_cost_and_hr.sql` — 원가 칸만 닫고,
  사무실·관리자는 `product_costs()` 함수로 예전과 똑같이 봅니다.
- **격리 DB에서 검증했습니다** — 적용 전 병원이 `5200`을 읽었고, 적용 후 차단됐으며,
  관리자는 `product_costs()`로 여전히 `5200`을 받습니다.
- ⚠ **아직 실행하지 않았습니다.** 승인 후에 진행합니다.

### 🟡 MEDIUM 1건 — 현장 직원이 **동료의 4대보험 자격취득일**을 읽습니다

```
GET /rest/v1/staff?select=name,position,insured_from,insurance
→ 홍현주(이사) 2023-05-01 · 오대성 2023-06-01 · 김진환 2025-12-01 …
```

업무에 필요 없는 개인정보입니다. 이름·담당은 오늘 누가 어디 가는지 알아야 하니
그대로 두고, **보험 칸 둘만** 닫습니다. 같은 제안 파일에 들어 있습니다.
적용 후에도 현장은 직원명부 7줄을 그대로 봅니다(업무 안 막힘).

### 🟢 LOW 1건 — 로그아웃해도 접근 토큰이 최대 60분 살아 있습니다

이건 **우리 결함이 아니라 JWT의 성질**입니다. 확인한 것:

- 로그아웃 응답 204 · **새로고침 토큰은 즉시 죽습니다** ✓
- 브라우저는 저장된 토큰을 지우므로 **뒤로가기로 자료가 다시 안 보입니다** ✓ (실제로 확인)
- 남는 위험은 「누가 토큰을 복사해 뒀을 때 최대 60분」뿐입니다

줄이시려면 Supabase 프로젝트 설정에서 JWT 만료를 낮추면 됩니다(기본 3600초).
**파일럿을 막는 문제는 아닙니다.**

---

## 새지 않은 것 (전부 실제 토큰으로 확인)

### 현장 계정 — 돈이 **한 건도** 안 샙니다

| 물어본 것 | 서버 응답 |
|---|---|
| 거래처 전체 `select=*` | **403 거절** |
| 거래처 돈 칸 8종 (단가·월정액·결제조건·사업자·세금·부가세…) | **전부 403 거절** |
| 월 실적(매출·원가·이익) · 청구 · 미수금 · 입금 · 단가 · 매출보정 · 운영비 · 세금계산서 · 문서함 · 영업리드 | **전부 빈 배열** |
| 상품 원가 | 빈 배열 |
| `clients_full()` 전체 내려받기 | 거절 (관리자만) |
| 다른 사람 프로필 | 본인 1줄만 |
| 업무용 (이름·주소·전화 · 일정 · 차량 · 재고) | 정상 — **업무는 안 막힙니다** |

### 병원 계정 — 다른 병원 자료가 **한 건도** 안 보입니다

⚠ 이 검사가 **공허하지 않다는 것**부터 확인했습니다. 남의 병원에 실제로
수거일정 17줄 · 요청 1줄 · 자재공급 11줄이 **있는데** 병원 계정은 0줄을 받았습니다.

| 물어본 것 | 서버 응답 |
|---|---|
| 남의 병원을 id로 콕 집어 조회 (거래처·일정·요청·주문·자재·청구·단가·월실적·메모·문서함) | **전부 0줄** |
| 필터 없이 통째로 (일정·요청·주문·자재·메모·청구·수거이벤트) | 받은 줄이 **전부 내 병원 것** |
| 회사 내부 (직원명부·운영비·차량·감사로그·영업리드·입금·오류기록) | **전부 0줄** |
| 다른 사람 프로필 | 본인 1줄만 |

> 다만 `site_notes`는 **어느 병원에도 자료가 없어** 격리를 증명하지 못했습니다.
> 파일럿에서 현장 메모가 쌓이면 다시 확인하겠습니다.

### 로그인 · 세션

| | 결과 |
|---|---|
| 틀린 비밀번호 | 400 거절 (토큰 안 줌) |
| 없는 계정 | 400 거절 — **틀린 비밀번호와 같은 답** (계정 존재 여부가 안 새어 나감) |
| 가짜 토큰 · 익명 | 401 / 42501 거절 |
| 헤더로 역할 바꿔치기 (`Role: service_role`) | **무시됨** — 청구 안 나옴 |
| 새로고침 | 로그인 유지 |
| 역할별 첫 화면 | 현장 → 오늘 일정 · 병원 → 포털 |

### 권한 없는 URL 직접 입력 — **전부 차단**

현장 9곳(`/receivables` `/billing` `/reports` `/revenue` `/bank` `/pricing` `/users` `/settings` `/performance`)
병원 9곳(위 + `/clients` `/today`) — **모두 「접근 권한이 없는 화면입니다」**

> ⚠ 처음에 제가 「주소가 안 바뀌었으니 열린 것」으로 잘못 셌습니다.
> 라우터는 **주소를 그대로 둔 채 차단 화면**을 띄웁니다. 차단 문구로 다시 쟀습니다.

### 돈·수량 중복 (격리 DB · 운영과 같은 migration)

| 검사 | 건수 | 실패 |
|---|---|---|
| 동시 실행 · 이중 저장 · 이중 차감 · 이중 확정 · 이중 입금 · 재시도 | 268 | **0** |
| 전체 DB 회귀 | 1,321 | **0** |

### 화면

| | 화면 | 가로 밀림 | 세로 늘어짐 | 화면 밖 | 글자 잘림 | 44px 미만 |
|---|---|---|---|---|---|---|
| 폰 390px (보통·큰 글씨) | 26 × 2 | 0 | 0 | 0 | 0 | 0 |
| PC 1440px | 26 | 0 | 0 | 0 | 0 | — (마우스라 해당 없음) |

---

## 실계정 모바일 E2E

### 현장 (`kjg@beonemirae.com` · 390px)

로그인 → 오늘 일정 → 병원 고르기 → 수거량 → 용기/자재 → 차량 → **저장 버튼이 눌리는 상태**까지 확인.
`＋` 버튼 49px · 가로 밀림 0 · 오늘 일정에 금액 0. **운영 DB에 한 줄도 안 썼습니다.**

- ⚠ **오늘 살아 있는 수거 예정이 0건**이라 「오늘 일정 → 병원」 경로는 못 밟았습니다 (DATA)
- ⚠ **네 명 모두 차량이 안 묶여 있어** 매번 차량을 고릅니다 (DATA)

### 병원 (`theone@beonemirae.com` · 390px)

로그인 → 포털 → 우리 병원 연결 확인 → 수거 요청 창 → 자재·용기 화면까지 확인.
「수거 요청」 358px · 「자재·용기 요청」 535px로 **둘 다 첫 화면 안**. 가로 밀림 0.

- ⚠ **상품 판매가가 전부 0원이라 주문할 물건이 하나도 안 뜹니다** (DATA)
  화면은 「추천할 사용 이력이 아직 충분하지 않습니다」라고 **정직하게** 말합니다.

---

## 운영 DB · 환경

| | 값 | 판정 |
|---|---|---|
| DB schema_version | 63 | 앱 기대값 63 — **일치** |
| 최신 migration | 0063 | 적용됨 |
| RLS 꺼진 표 | 없음 | 통과 |
| 정책 없는 표 | 없음 | 통과 |
| 필요한 함수 16종 | 전부 있음 | 통과 |
| 번들에 secret/service_role | 없음 | 통과 (`sb_secret_`는 supabase-js **내부 방어 코드**) |
| demo 모드 | 이중 잠금 (`VITE_DEMO_MODE=1` **그리고** Supabase 미설정일 때만) | 항상 꺼짐 |
| 익명 접근 | 42501 거절 | 통과 |
| 자가진단 (`app_health_check`) | **관리자 권한 필요 — 저는 못 봤습니다** | 대표님 확인 필요 |

---

## 제가 확인하지 **못한** 것 (정직하게)

1. **admin / office 계정으로는 아무것도 확인하지 못했습니다.**
   제게 있는 실계정은 병원 1 · 현장 4뿐입니다.
   - `office` 계정이 없어 「사무실이 봐야 할 것을 다 보는가 / 관리자 것을 안 보는가」를 못 쟀습니다
   - `admin` 계정이 없어 자가진단·계정 목록·감사로그를 못 봤습니다
2. **현장메모(site_notes) 격리** — 어느 병원에도 자료가 없어 증명이 안 됐습니다
3. **실제 저장(WRITE)** — 운영 DB를 건드리지 않으려고 저장 버튼 **직전까지만** 밟았습니다

---

## 남은 파일

| 파일 | 무엇 |
|---|---|
| `supabase/checks/CHECK_PILOT_SAFETY.sql` | **읽기 전용** 운영 DB 점검 (대표님이 SQL Editor에서 Run) |
| `supabase/proposals/PROPOSAL_0064_cost_and_hr.sql` | HIGH·MEDIUM 수정안 — **승인 전까지 실행 안 함** |
