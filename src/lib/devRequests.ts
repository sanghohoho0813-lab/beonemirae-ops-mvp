import type { UserRole } from '../context/AuthContext'

// ─────────────────────────────────────────────────────────────────────────────
// 개발자에게 요청하기 — 역할별 점검 항목
//
//  빈 칸만 주면 대부분 아무것도 쓰지 않습니다. 쓰더라도 "불편해요" 같은 말이
//  남아 무엇을 고쳐야 하는지 알 수 없습니다. 그래서 역할마다 다섯 가지를
//  미리 적어 두고 고르게 합니다. 자유 의견은 그 아래에 따로 받습니다 —
//  목록에 없는 것이 늘 있습니다.
//
//  ── 항목을 고른 기준 ────────────────────────────────────────────────────
//
//  한 번 고치면 끝나는 것이 아니라 **앞으로도 계속 확인해야 하는 것**으로만
//  채웠습니다. 사람이 바뀌고 거래처가 늘면 다시 어긋나는 것들입니다.
//
//    거래처 정보가 실제와 맞는가   담당자가 바뀌고 주소가 바뀝니다
//    청구가 기존 엑셀과 맞는가     단가가 바뀌고 규격이 늘어납니다
//    같은 것을 두 번 입력하는가    화면이 늘면 다시 생깁니다
//
//  "버튼 색을 바꿔 주세요" 같은 한 번짜리 요청은 넣지 않았습니다. 그런 것은
//  자유 의견에 적으면 됩니다. 목록은 분기에 한 번씩 눌러 보는 점검표로
//  쓰이는 것이 목적입니다.
//
//  고른 문구는 DB 에 **그대로** 저장합니다(0022). 여기 문구를 나중에 고쳐도
//  예전 요청이 무슨 뜻이었는지 남아야 하기 때문입니다.
// ─────────────────────────────────────────────────────────────────────────────

export const DEV_REQUEST_TOPICS: Record<UserRole, string[]> = {
  field: [
    '거래처 정보가 실제와 다릅니다 (주소·담당자·연락처·수거주기)',
    '오늘 일정에 없는 곳을 다녀왔습니다',
    '폰에서 입력이 느리거나 중간에 끊깁니다',
    '자재 규격·수량이 실제 쓰는 것과 맞지 않습니다',
    '차 안에서 누르기 어려운 버튼·작아서 안 보이는 글자가 있습니다',
  ],
  office: [
    '청구 금액이 기존 엑셀과 다릅니다',
    '병원 요청을 처리하는 중에 막히는 단계가 있습니다',
    '병원이 요구하는 항목이 운영 리포트에 없습니다',
    '같은 내용을 두 번 이상 입력하고 있습니다',
    '단가·계약 조건을 바꾸기가 어렵습니다',
  ],
  admin: [
    '대시보드에서 보고 싶은 지표가 빠져 있습니다',
    '실사·인증 대응에 필요한 자료가 부족합니다',
    '정산·청구 규칙을 바꿔야 합니다',
    '계정·권한 관리에 필요한 기능이 있습니다',
    '외부 연동이 필요합니다 (올바로·회계·문자·메일)',
  ],
  //  병원 계정은 포털의 「요청」으로 보냅니다. 이 화면 자체가 열리지 않습니다.
  client: [],
}

/** 이 역할이 개발 요청을 보낼 수 있는지 (내부 직원만) */
export function canSendDevRequest(role: UserRole | null): boolean {
  return role === 'admin' || role === 'office' || role === 'field'
}

export const DEV_REQUEST_STATUSES = ['접수', '확인', '처리 완료', '보류'] as const
export type DevRequestStatus = (typeof DEV_REQUEST_STATUSES)[number]

/** 상태별 뱃지 색 — 처리 완료만 가라앉히고 나머지는 눈에 띄게 둡니다 */
export const DEV_STATUS_STYLE: Record<DevRequestStatus, string> = {
  접수: 'bg-teal-50 text-teal-700',
  확인: 'bg-amber-50 text-amber-700',
  '처리 완료': 'bg-accent-100 text-accent-800',
  보류: 'bg-navy-100 text-navy-500',
}
