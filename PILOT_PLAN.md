# PILOT_PLAN — 이번 주 Pilot (0122)

> Material Usage + 5~10 Customer Pilot + Automatic Evidence
> 신규 기능 확장 없음. 아래는 **이미 있는 화면으로 1주일 실제 기록을 쌓는 계획**입니다.
> ⚠ 여기 적힌 숫자 중 「목표 %」는 없습니다. 성과는 쌓인 뒤 건수로 읽습니다.

---

## OBJECTIVE

1주일 동안 Pilot 거래처(월정액 5~10곳)에서 **실제 수거 입력**이 시스템에 쌓이고,
그 입력 한 번이 **수거이력 · 거래처 화면 · 자재 사용 기록 · Pilot 요약**으로
자동 연결되는 것을 실제 기록으로 보여 줍니다. 사람이 따로 적는 Evidence 는 없습니다.

## SCOPE

| 항목 | 값 |
|---|---|
| 거래처 | 월정액 거래처 **5~10곳** (PHASE 1). 「10」은 권장이지 상한이 아닙니다 |
| 현장 | 현장 직원 **1명** (본인 계정 · 차량 지정) |
| 확인 | 대표 · 이사 — 성과 화면 「이번 주 Pilot 실제 기록」 카드 (관리자만 보임) |
| 포털 | 병원 **1~2곳**이 포털에서 직접 요청을 올려 봅니다 (실제 요청만 셉니다) |
| 시작일 | 설정 → 도입 전 기준값 → **실증 시작일** (Pilot 시작일로 함께 쓰입니다) |
| 켜는 법 | 거래처 관리(관리자) → 카드 아래 「이번 주 PILOT」 체크 → 부제에 「Pilot N / 10」 |
| 현장 화면 | 따로 「Pilot 모드」 없음. 오늘 일정·거래처 상세에 **PILOT 배지** 하나만 |

### 준비 (대표님)
1. `supabase/proposals/PROPOSAL_0122_pilot_clients.sql` 을 Supabase SQL Editor 에서 1회 실행 (칸 하나 추가, 지우는 것 없음).
2. 설정 → 실증 시작일 = Pilot 첫날.
3. 거래처 관리에서 월정액 거래처 5~10곳에 「이번 주 PILOT」 체크.
4. 현장 직원 계정에 차량이 묶여 있는지 확인(기사 이름이 기록에 남습니다).

## PERIOD

**7일** (시작일 ~ 시작일+6). 요약 카드는 시작일 ~ 오늘을 셉니다.
시작일이 비어 있으면 카드는 **오늘 하루만** 세고 「시작일 미설정」이라고 적습니다 — 임의 기간을 만들지 않습니다.

## 현장이 하는 일 (평소와 같음 + 한 구역)

오늘 일정 → 병원 → 수거 입력 → 수거량 → (필요하면) **「이번 수거 자재 사용량」** 접힌 구역을 열어
병원이 이번에 실제 쓴 규격을 ± 로 센다 → 저장. 끝.
- 구역은 기본 접힘. 그 병원에서 최근/이전에 적힌 규격만 펼쳐지고 나머지는 「나머지 규격 N개 보기」.
- 「주고 온 자재(공급)」와 다른 구역입니다. 사용량은 **재고를 움직이지 않습니다.**
- 4칸(가져온 용기)을 안 적었으면 규격별 합계로 자동 채워집니다 — 같은 것을 두 번 적지 않습니다.
- 자릿수가 이상하면(평소와 크게 다름 · 1,000개 이상) 저장 전에 한 번 묻습니다. 막지 않습니다.

## SUCCESS EVIDENCE (자동으로 쌓이는 것만)

| 항목 | 어디서 나오나 | 출처 표기 |
|---|---|---|
| AX 수거 처리건수 | collection_events (Pilot 거래처 · 기간 · 시연/취소/시작일 이전 제외) | COLLECTION TABLE |
| 자재사용 기록률 | 규격별 사용량이 적힌 수거 / Pilot 완료 수거 | MATERIAL USAGE |
| 직원 사용 | 이벤트에 남은 입력자 (없으면 일정의 기사 이름) 수 | COLLECTION TABLE |
| 거래처 Coverage | Pilot 거래처 중 기간 안 입력 1건 이상인 곳 / Pilot 거래처 | COLLECTION TABLE |
| Portal Self-Service | client_requests.source = 'portal' (Pilot 거래처 · 기간) 건수 · 처리 건수 · 병원 수 | CUSTOMER REQUEST |
| Re-entry Reduction Proxy | 취소 후 재입력 건수 / 입력 건수 — **줄었다가 아니라 건수** | COLLECTION TABLE |
| Data Connection | 입력 → 완료 일정(수거이력·거래처 화면)으로 연결된 건수 | COLLECTION TABLE |
| 규격별 사용 · 공급 | usedItems 합계 · materials.items 합계 (남은 개수가 아님) | MATERIAL USAGE |
| 세지 않은 것 | 시연 · 시작일 이전 · 취소 · Pilot 외 거래처 — 건수를 그대로 적음 | — |

### 자동으로 재지 못하는 것 — BASELINE KNOWN / UNKNOWN
카톡·전화 응대 시간, 엑셀 정리 시간, 같은 정보 반복 입력 횟수, 월간 문서 시간, 누락·재확인 건수, 하루 처리 건수.
→ 설정 「도입 전 기준값」에 **실측**이 있으면 KNOWN, 없으면 UNKNOWN 으로 비워 둡니다. 시연용 예시값은 KNOWN 으로 치지 않습니다.
「35% 향상」 같은 개선율은 이 카드가 만들지 않습니다. 사용자 피드백(USER FEEDBACK)은 시스템 기록과 합치지 않고 자리만 알려 줍니다.

## PHASE (기록용)

| PHASE | 대상 | 상태 |
|---|---|---|
| 1 | 월정액 거래처 5~10곳 | **이번 주** |
| 2 | 단순 거래처 (단일 성상 · 주 1~2회) | 예정 |
| 3 | kg 단가 거래처 | 예정 |
| 4 | 박스 개당 · 자재 유상 · 복합 거래처 | 예정 |

## AX Owner (역할 — 새 권한 체계 없음)

기존 `admin` 역할이 그대로 AX Owner 입니다. 권한 표(`access.ts`)·RLS 는 바꾸지 않았습니다.
- Pilot 거래처 켜고 끄기 (거래처 관리)
- Pilot 시작일 (설정 · 실증 시작일)
- Pilot 요약 확인 (성과 · 요약 탭 맨 위)
- 수거 기록 고치기 (기존 amend 정책 — 사유 필수, 한 트랜잭션)
- 매일: 요약 카드에서 「세지 않은 것」과 「BASELINE UNKNOWN」을 확인하고, 필요한 기준값만 실측으로 넣기

## 하지 않는 것 (이번 주)
53곳 전체 전환 · 현장용 별도 Pilot 모드 · 개선율 자동 계산 · 월 환산 · 자재 추천/예측 · SQL 함수 변경 · 기존 화면 재구성.
