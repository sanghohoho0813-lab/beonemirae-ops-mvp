# 파일럿 전 남은 것 (TODO · Blocker)

> 이 문서는 **아직 끝나지 않은 것**만 적습니다. 끝난 것은 지웁니다.
> 「했다고 적어 두고 실제로는 안 된 것」이 남지 않게 합니다.

---

## A. 대표님이 PC 앞에서 확인하실 것

### A-1. 현장 계정 PC ↔ 모바일 전환  ⚠ 확인 대기

**상태: 코드는 고쳤고 자동 검사 32건이 통과했습니다. 사람 눈 확인만 남았습니다.**

- 무엇이었나 — PC 왼쪽 아래 「모바일 화면」 단추가 현장 계정에도 **보이는데,
  누르면 「접근 권한이 없는 화면입니다」**가 떴습니다. 관리자는 됐습니다.
- 원인 — `/mobile-preview` 가 admin·office 전용이었습니다 (`src/lib/access.ts`).
  게다가 미리보기 틀이 `/`(대시보드)를 띄우는데, 대시보드도 현장에게 안
  열립니다 — 권한만 열었다면 이번엔 **틀 안이** 막혔을 것입니다.
- 고친 것 — ① 현장에게 `/mobile-preview` 를 엽니다 ② 틀 안은 각자 첫 화면
  (현장은 오늘 일정)을 띄웁니다 ③ 못 여는 역할에게는 그 단추가 아예 안
  보입니다. **보이면 눌리고, 눌리면 열려야 합니다.**
- 자동 검사 — `test/browser/check_viewswitch.mjs` (32검사). 전환 · 되돌아오기 ·
  새로고침 · 뒤로가기 · 왕복 2회 · 폰에서 PC 화면 보기 · 재로그인.
  권한 한 줄을 되돌리면 이 검사가 FAIL 합니다 (빈 검사가 아님을 확인).

**PC 앞에서 확인하실 순서** — 현장 계정으로 로그인 →
1. 왼쪽 아래 「모바일 화면」 누르기 → 폰 틀이 뜨고 **틀 안에 오늘 일정**이 보이는가
2. 「웹 화면으로 보기」로 되돌아오는가
3. 그 상태에서 새로고침 → 그대로인가 · 뒤로가기 → 오늘 일정으로 오는가
4. 로그아웃 후 다시 로그인 → 처음부터 모바일로 시작하는가

> ⚠ 이 항목은 **대표님이 직접 보시기 전까지 「끝났다」고 적지 않습니다.**
> 자동 검사는 시늉본 서버 위에서 돕니다 — 실제 배포판·실계정에서 한 번은
> 사람 눈으로 봐야 합니다.

---

## B. 자료를 넣어야 시작되는 것

| 남은 것 | 없으면 어떻게 되나 | 어디서 |
|---|---|---|
| **`PROPOSAL_0073_day_close.sql` 실행** (0088) | 기사님이 「오늘 업무 마감」을 못 찍음 → 저녁마다 카톡이 그대로 남음. 되돌리기 사유도 안 남음 | Supabase SQL Editor. 판 70 → 73, 지우는 것 없음 |
| 거래처 11곳 **주소·전화** | 기사님이 주소 없이 다님 · 전화 못 검 | `FILL_client_contact.sql` |
| 거래처 **수거 주기** | 11곳 전부 「주 1회」인데 엑셀과 안 맞음 (해올 5일 · 신세계서울 3일 · 목동현대웰 3일) — 일정 자동 편성이 어긋남 | 대표님이 실제 계약 주기를 알려 주시면 반영 |
| 파일럿 병원의 **향후 일정** | 「앞으로 갈 곳」이 비어, 미리 보고 다니는 것을 확인 못 함 | 오늘 일정 → 달력 → 일정 추가 |
| 그 병원의 **담당 기사 배정** | 일정이 기사 전원에게 보임 (업무는 되지만 「내 일정」이 안 됨) | 거래처 상세 → 담당 기사 |

> 담당 기사를 아무도 배정하지 않으면 **모든 기사에게 보입니다.** 결함이 아니라
> 정한 규칙입니다 — 한 명이라도 배정하는 순간 그 사람에게만 보입니다.

---

## C. 파일럿 후로 미루는 것

- 폴드폰·태블릿 폭(673~768px) 정리 — 작은 글자 6개 · 44px 미만 터치영역 23개.
  **일반 스마트폰(390·412px)은 0개**라 파일럿을 막지 않습니다.
- 「한 병원은 늘 같은 기사인가」가 확인되면 주차별 정리 · 같은 날 순서 조정
- 요청 처리 알림 (지금은 화면을 열어야 보입니다)
- 인터랙티브 가이드 확장 (짚을 자리 14곳은 이미 준비돼 있습니다)

---

## C-2. 확인해 둔 것 (파일럿을 막지 않음)

아래는 **이미 기준을 넘긴** 것들입니다. 다시 손볼 필요가 없다는 기록으로 남깁니다.

| 본 것 | 잰 값 (390 · 412px) |
|---|---|
| 작은 글자 | **0개** (최소 16px) |
| 흐린 회색 (대비 미달) | **0개** |
| 44px 미만 터치영역 | **0개** |
| 붙어 있는 터치영역 | **0쌍** |
| 가로 스크롤 | **0px** |
| 수거 입력 — 기사·차량 고르는 칸 | **0개** (계정 기본값 자동) |
| 수거 입력 — 자재·용기 | 접힘 5개 **전부 접힌 채** 시작 |
| 저장 연타 5번 | 서버로 **1건** |

**도움말(사용 방법)** — 실제 화면을 따라 움직이는 방식이고, 뒷화면을 멈추지
않습니다. 안내 4개가 각각 3 · 5 · 4 · 3단계(3~6 범위), 설명 15줄이 **전부
60자 이하**(가장 긴 줄 53자), 세 문장 이상인 줄은 없습니다.

---

## C-3. 0088 에서 닫은 것 (파일럿을 막던 자리)

| 닫은 것 | 확인 |
|---|---|
| 오늘 업무 마감 — 기사님께 **다시 입력받지 않음** (서버가 셈) | `check_dayclose` 22 · `db_pilot73` |
| 되돌리기 **사유** — 수거 입력·수거이력이 같은 부품 하나 | `check_revertreason` 18 |
| 공용차 **반납 지연** — 밀린 예약을 놓을 길이 아예 없던 것 | `check_truck` 31 |
| **확정 청구에 들어간 수거 되돌리기 차단** — 화면이 약속하는데 서버에 없던 잠금 | `db_pilot73` (우회로 없음까지) |
| 사무실 재고를 설정 → 자재 관리 화면 맨 위로 | — |

자세한 것은 `docs/PILOT_READY_0088.md`.

---

## D. 이번 작업에서 손대지 않는 것

소모품 판매 · 매출 AX · 홈택스 · 경로최적화. 대표님 지시입니다.
