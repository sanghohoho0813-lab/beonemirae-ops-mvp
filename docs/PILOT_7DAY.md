# 파일럿 7일 — 측정 계획

㈜비원미래 의료폐기물 수거 운영관리 실증. 7일간 무엇을 어떻게 재는지 한 장에 둡니다.

---

## 1. 자동으로 쌓이는 값 (손으로 적을 필요 없음)

아래 네 가지는 **이미 시스템에 기록되고 있습니다.** 파일럿을 위해 새로 만든 것이
아니라, 원래 남던 기록에서 계산만 합니다.

| 값 | 어디서 나오는가 | 뽑는 법 |
|---|---|---|
| 수거 입력 소요시간 | `collection_events.input_duration_ms` — 입력 화면에 들어간 순간부터 저장까지 | 아래 SQL |
| 당일 입력률 | `schedules.date` 와 `completed_at` 비교 | 아래 SQL |
| 일정 추가·변경 | `audit_logs.action` (`schedule.book`·`move`·`cancel`) | 아래 SQL |
| 요청 처리시간 | `client_requests.handled_at − created_at` | 아래 SQL |

**뽑는 법** — Supabase → SQL Editor 에 `supabase/proposals/PILOT_METRICS.sql`
붙여넣고, 맨 위 날짜 두 줄만 파일럿 기간으로 바꿔 Run. **읽기만 하고 아무것도
바꾸지 않습니다.** 하루에 한 번 돌려 표를 캡처해 두시면 7일치가 모입니다.

> 시간대는 한국(Asia/Seoul)으로 계산합니다. UTC 로 세면 밤에 입력한 것이 다음
> 날로 넘어가 당일 입력률이 실제보다 낮게 나옵니다.
>
> 시연용으로 넣은 입력(`demo_session_id`)은 빼고 셉니다.

---

## 2. 손으로 재는 값 (3일)

아래 네 가지는 **시스템 밖에서 일어나는 일**이라 기록이 남지 않습니다. 이것을
재려고 새 기능을 만들지 않습니다 — 3일만 손으로 적습니다.

> ⚠ 왜 3일인가: 7일 내내 적으면 적는 것 자체가 일이 되어 중간에 포기합니다.
> **파일럿 시작 전 3일(도입 전)** 과 **파일럿 중 3일**을 같은 방식으로 적어
> 견주는 것이 목적입니다. 하루 다 채우지 못한 날은 「미측정」으로 남기고
> 빈칸을 지어내지 않습니다.

### 적는 표

| 날짜 | ① 카톡 횟수 | ② Excel 재사용 | ③ 전화 재확인 | ④ 이사님 재정리 |
|---|---|---|---|---|
| / (도입 전 1일차) |  |  |  |  |
| / (도입 전 2일차) |  |  |  |  |
| / (도입 전 3일차) |  |  |  |  |
| / (파일럿 1일차) |  |  |  |  |
| / (파일럿 2일차) |  |  |  |  |
| / (파일럿 3일차) |  |  |  |  |

### 무엇을 한 건으로 세는가

정의를 먼저 못박아 둡니다. 사람마다 다르게 세면 앞뒤 비교가 무의미해집니다.

**① 카톡 횟수** — 수거 업무 때문에 오간 카톡 **메시지 묶음**을 셉니다.
- 세는 것: 「오늘 어디 가요?」 「○○병원 몇 시?」 「그거 했어요?」 처럼 **일정·수거·자재를 묻거나 알리는** 대화
- 한 건 기준: 한 주제에 대해 오간 대화 한 덩어리 = 1건 (메시지 20개가 오가도 한 주제면 1건)
- 빼는 것: 인사·잡담·회식 이야기
- 적는 사람: 사무실(이사님)

**② Excel 재사용** — 같은 자료를 **엑셀에 다시 옮겨 적은** 횟수.
- 세는 것: 이미 어딘가 있는 숫자를 엑셀에 다시 치거나 복사해 붙인 일 (파일 1개 작업 = 1건)
- 빼는 것: 새로 생긴 자료를 처음 입력하는 것
- 적는 사람: 사무실(이사님)

**③ 전화 재확인** — **이미 있어야 할 정보**를 몰라서 건 전화.
- 세는 것: 「그 병원 몇 kg이었죠?」 「오늘 갔어요?」 「주소가 어디죠?」
- 빼는 것: 병원과의 상담·영업 통화, 새 일을 만드는 통화
- 적는 사람: 사무실·기사 각자, 하루 끝에 합산

**④ 이사님 재정리 시간** — 기사님이 준 것을 **다시 정리하는 데 쓴 시간(분)**.
- 세는 것: 손으로 적어 온 것을 옮기기, 카톡 사진 보고 표에 넣기, 빠진 것 확인하기
- 빼는 것: 청구서 만들기, 입금 확인 같은 원래 사무 업무
- 적는 사람: 이사님

---

## 3. 파일럿 판정 기준

숫자를 모은 다음 무엇을 보고 「됐다/안 됐다」 할지 **미리** 적어 둡니다.
끝나고 나서 정하면 나온 숫자에 맞춰 기준을 움직이게 됩니다.

| 보는 것 | 파일럿이 성공이라면 |
|---|---|
| 당일 입력률 | 기사님이 그날 갈 곳을 **그날 안에** 입력 |
| 수거 입력 소요시간 | 한 건 입력이 **길어지지 않음** (설명 없이 끝남) |
| 카톡·전화 재확인 | 도입 전 3일보다 **줄어듦** |
| 이사님 재정리 시간 | 도입 전 3일보다 **줄어듦** |
| 요청 처리시간 | 병원 요청이 **묻히지 않고** 처리됨 |

> ⚠ 실제 판매나 매출이 발생하기 전에는, 예상 실적이나 추정 절감액을 실제
> 성과처럼 적지 않습니다. **잰 것만** 적습니다.

---

## 4. 파일럿 규모

- 기사 1명 · 차량 1대 · 병원 1곳으로 시작합니다.
- 향후 일정이 미리 들어 있어야 「일정을 미리 보고 다닌다」를 확인할 수 있습니다.
- 나머지 기사·차량·병원은 이 1건이 도는 것을 본 뒤 늘립니다.
